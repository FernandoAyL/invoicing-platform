# To code review

Tasks that passed QA and await final code review. On approval the planner moves
the task to `DONE.md` (flipping `☐ → ☑`); on rejection it goes back to `TODO.md`
with findings attached, and must pass QA again before returning here.

Keep the original task ID. Format:

```markdown
- ☐ `10005` **Title** — original description.
  - **Done:** <developer summary>.
  - **QA:** <QA pass summary>.
```

- ☐ `30026` **Harden QBO natural-key query-string construction against malformed input** — external checker flagged `upsertLink` for SQL injection; that claim doesn't hold (Drizzle-parameterized, no raw SQL). The real gap was the hand-rolled QBO query `where`-clause builder in `retry-sweep.ts` (QBO's query API has no parameterized mechanism, so interpolate-and-escape is the only option): the `TxnDate` fallback branch skipped `escapeQboString` entirely.
  - **Done:** fixed the one-line gap (`TxnDate = '${escapeQboString(txn.txnDate)}'`, `apps/api/src/qbo/retry-sweep.ts:87`); audited `escapeQboString` — backslash-then-quote escaping in that order matches QBO's documented query-language string-literal rule (developer.intuit.com SQL query syntax), no other special characters apply; extracted two pure exported functions, `buildDocumentCreateWhere`/`buildContactCreateWhere` (plus exporting `escapeQboString` itself), so the fix is unit-testable without a DB round-trip — `txn.txnDate` is a real Postgres `date` column so this specific gap was defense-in-depth, not a live exploit (stated plainly, not overclaimed). Extended `FakeQboCall`/`queryEntities` in `apps/api/src/__tests__/helpers/fake-qbo-write-client.ts` to capture the `where` string. Added unit tests for `escapeQboString` (quote, backslash, backslash-then-quote ordering, empty string) and both `buildXWhere` functions (adversarial docNumber/email/displayName/txnDate with embedded quotes+backslashes, `WHERE_SHAPE` regex proving no early string-literal termination, falsy-fallback behavior unchanged) plus one integration-level regression test through `runOutboundRetrySweep` with a `docNumber` containing an embedded quote, asserting the fake client's captured `where` is exactly the expected escaped string — all in `apps/api/src/qbo/retry-sweep.test.ts`. Manually verified the regression guard is real: reverted the one-line fix, confirmed the new adversarial `TxnDate` test fails with the exact expected-vs-actual mismatch, restored and confirmed green again. Full API suite: 559/559 passing; `pnpm -r typecheck` and targeted Biome checks clean.
  - **QA:** Passed. Independently reviewed the diff (TxnDate branch now escaped; DocNumber/PrimaryEmailAddr.Address/DisplayName branches unchanged; `buildDocumentCreateWhere`/`buildContactCreateWhere` confirmed wired into `reconcileDocumentCreate`/`reconcileContactCreate`, not orphaned) and confirmed via grep that no other `apps/api/src/qbo/*.ts` file builds a QBO `where` string. Ran `retry-sweep.test.ts` directly (22/22). Did an independent break-fix-restore on the `TxnDate` escaping specifically — reverted the one line, the new adversarial test failed with a genuine expected-vs-actual mismatch (not a crash, not vacuous), restored, green again. Manually verified the `WHERE_SHAPE` regex actually discriminates escaped vs. unescaped adversarial input rather than trusting the test's use of it. Confirmed `transactions.txnDate` is a real Postgres `date` column (`schema.ts:156`), matching the plan's non-exploitable/defense-in-depth framing — the Done note doesn't overclaim it as a live vuln. Full suite 559/559 (`--maxWorkers=2`), `pnpm -r typecheck` clean, targeted Biome clean.
