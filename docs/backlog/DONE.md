# Done

Completed and verified tasks. Keep the original task ID.

## Phase 0 — Design & foundations (`0000x`)

- ☑ `00001` Architecture decisions doc (`docs/architecture-decisions.md`)
- ☑ `00002` Product requirements doc (`docs/PRD.md`)
- ☑ `00003` Project instructions (`CLAUDE.md`)
- ☑ `00004` Backlog structure (`docs/backlog/`: TODO, TOCODEREVIEW, TOTEST, DONE)
- ☑ `00005` Register QuickBooks Online developer account + sandbox company
- ☑ `00006` Create QBO app: OAuth client id/secret, redirect URIs, webhook endpoint
- ☑ `00007` AWS account + IAM for Terraform; S3 remote state backend with native locking (`use_lockfile`)
- ☑ `00008` Local dev env: `docker-compose` (postgres:17 + Node 24 app) + base tooling
  - **Delivered:** `docker-compose.yml` (Postgres 17 with healthcheck + `db-data` volume; app gated on db health, source bind-mount + anonymous `node_modules` volume for hot reload), `Dockerfile` (node:24 + Corepack/pnpm), minimal `src/index.ts` (`node:http` `/health` that pings Postgres via `pg`), `package.json` + `packageManager` pin, `tsconfig.json`, `.env.example`, `.dockerignore`, and Biome (`biome.json`, 2-space to match pnpm, `format`/`lint`/`check`/`ci` scripts). Verified: `docker compose up --build` → `GET /health` → `200 {"status":"ok","db":"up"}`; `tsc --noEmit` and `biome ci` clean.
- ☑ `00009` Repo scaffolding remainder: pnpm workspace layout + typed `.env` config loader
  - **Delivered:** `pnpm-workspace.yaml` (`apps/*`, `packages/*`); backend at `apps/api` (`@invoicing/api`) with a dependency-free typed env loader (`apps/api/src/config.ts`); shared `tsconfig.base.json` extended per package; root `package.json` as workspace root. Verified: `docker compose up --build` → `/health` `200`; `pnpm -r typecheck` + `biome ci` clean.

## Phase 1 — Core app + CI (`1000x`)

- ☑ `10001` Drizzle schema + first migration — accounting core
  - **Delivered:** `apps/api/src/db/schema.ts` — 11 tables + 8 pg enums (unified `transactions` + `transaction_lines` + `ledger_entries`, `contacts` role flags, chart-of-accounts `accounts`, entity-typed `sync_links`). Money `numeric(14,2)`, uuid PKs, org-scoped FKs, self-ref `accounts.parent_id`, cascade on lines/ledger. `drizzle.config.ts` + `drizzle/0000_young_siren.sql`; `db:generate` + programmatic `db:migrate` (`src/db/migrate.ts`). Verified: migration applies over docker network (11 tables + `drizzle.__drizzle_migrations`); `tsc --noEmit` + `biome ci` clean.
- ☑ `10002` CI (GitHub Actions) + Vitest setup
  - **Delivered:** `.github/workflows/ci.yml` (push to `main` + PRs): install → `biome ci` → `pnpm -r typecheck` → `pnpm -r test` → `docker build`. Vitest wired (`apps/api` + root `test` scripts, `vitest.config.ts`); `config.ts` refactored to testable `loadConfig(env)` + `config.test.ts` (4 cases). Verified: `vitest` 4/4, `biome ci`/typecheck clean, **CI green on GitHub**.
- ☑ `10003` Fastify server bootstrap
  - **Delivered:** replaced the `node:http` placeholder with Fastify. `src/app.ts` (`buildApp`, injectable pool for tests), `src/plugins/db.ts` (fastify-plugin decorating `pool` (pg) + `db` (drizzle), owns pool lifecycle via `onClose`), `src/routes/health.ts` (`GET /health` pings the pool), `src/index.ts` (listen on `0.0.0.0:config.port`, graceful SIGTERM/SIGINT). Pino structured logging (pretty in dev via pino-pretty, JSON otherwise). Unit tests `src/routes/health.test.ts` (200 up / 503 down via `app.inject` + fake pool). **Verified:** `docker compose up --build` → `GET /health` `200`; `vitest` 6/6, typecheck + `biome ci` clean; **CI green, merged to `main` via PR #8**.
- ☑ `10004` Auth: email/password session login, httpOnly cookie, logout, seeded users, Admin/Member roles
  - **Delivered:** DB-backed opaque-token sessions (`sessions` table + migration `0001_huge_newton_destine.sql`). `apps/api/src/auth/password.ts` (scrypt N=16384/r=8/p=1, `timingSafeEqual`, never throws on malformed hash), `auth/session.ts` (256-bit token, only SHA-256 hash stored, create/find/delete, expiry enforced). `plugins/auth.ts` (`@fastify/cookie` signed with `SESSION_SECRET`; `authenticate` + `requireRole` preHandlers; cookie `httpOnly`/`sameSite=lax`/`secure`-in-prod). `routes/auth.ts` (`POST /api/auth/login` — Fastify JSON schema, dummy-hash timing defense against user-enumeration, generic `401`; `POST /api/auth/logout` — real server-side revocation, idempotent; `GET /api/auth/me`). `db/seed.ts` (idempotent org + admin/member users, `db:seed`). `config.ts`/`.env.example`/`docker-compose.yml` wired for `SESSION_SECRET`/`SESSION_TTL_HOURS`/`SEED_*_PASSWORD`; `plugins/db.ts`/`app.ts` accept an injectable `db` for route tests. **Verified:** QA ran the full containerized `docker compose up --build` flow (boots, `/health` 200, seed x2 idempotent, login→`/me`→logout→`/me` 200/200/204/401 with session revoked); `vitest` 28/28, typecheck + `biome ci` clean; **review-approved** (2 non-blocking follow-ups filed). Delivered across 2 attempts (attempt-1 QA reject: compose env passthrough).
- ☑ `10005` Contact CRUD (customer role first): name + contact info, attachable to an invoice; maps to a QBO Customer
  - **Delivered:** org-scoped contact CRUD behind `app.authenticate` (both roles). `apps/api/src/contacts/service.ts` (`createContact`/`listContacts`/`getContact`/`updateContact`/`archiveContact`, every query filtered by `orgId`); `apps/api/src/routes/contacts.ts` (`POST`/`GET` list/`GET :id`/`PATCH`/`DELETE` with JSON schemas — uuid params, email format, `minLength`, PATCH `minProperties:1`, `additionalProperties:false`). Soft-archive via a new `is_active` column (migration `0002_married_cassandra_nova.sql`), QBO-style deactivate-not-delete; archive is idempotent, list excludes archived unless `?includeInactive=true`. Also disabled Fastify's default `removeAdditional` (`ajv.customOptions.removeAdditional:false` in `app.ts`) so `additionalProperties:false` truly 400s — an app-wide fix that also tightens the 10004 login schema. No QBO `SyncLink` / audit writes (deferred to Phase 2 / `10008`). **Verified:** QA ran full CRUD + validation + idempotent-archive against the docker-compose stack as both admin & member, proved cross-org isolation with a genuine second org (GET/PATCH/DELETE/LIST all 404/exclude), and regression-checked the `removeAdditional` change (login intact, now 400s on unknown field); `vitest` 45/45, typecheck + `biome ci` clean; **review-approved** (1 non-blocking nit: whitespace-only `displayName` trims to empty).
- ☑ `10008` Audit log write path: every mutating action appends to `SyncAuditLog` (entity, action, direction, outcome, user, timestamp)
  - **Delivered:** append-only audit on every local mutation, atomic with the change. Added `'local'` to the `sync_direction` enum (migration `0003_gorgeous_wolf_cub.sql`) so app-originated actions are logged distinctly from inbound/outbound QBO sync. New `apps/api/src/audit/service.ts` (`writeAuditLog(tx, entry)` + `AuditEntry`, defaults `direction:'local'`/`outcome:'success'`). `contacts/service.ts` create/update/archive refactored to `db.transaction(tx => …)` writing one audit row each on the same `tx` (attributed to `user.id`/`orgId` from the session — not spoofable); `routes/contacts.ts` passes `user.id`. Reads and no-row-matched no-ops write nothing; `detail` carries only changed field names. Tests: `audit/service.test.ts` + `contacts.test.ts` (one row per mutation, reads/404s write nothing, forced-failure rollback leaves neither contact nor audit row). **Verified:** QA on fresh `docker compose up --build` (migration 0003 applied, `enum_range`=inbound/outbound/local) psql-confirmed one correct row per create/update/archive, cross-user attribution (member vs admin), and zero rows from reads/no-ops; `vitest` 48/48, typecheck + `biome ci` clean; **review-approved**.
- ☑ `10011` Chart of accounts: seed minimal accounts (A/R, Sales Income, bank, Undeposited Funds) + balanced posting helper that rejects unbalanced transactions
  - **Delivered:** the ledger core. `apps/api/src/money.ts` — exact integer-cents `toCents`/`formatCents` (regex-parsed, no float path; rejects `>2` decimals / malformed / non-finite). `apps/api/src/ledger/posting.ts` — `postLedger(tx, {orgId, transactionId, entryDate, lines})`, transaction-aware (posts inside the caller's txn, no HTTP surface), validates each line (no negative / both-sides / both-zero / empty set) and throws `UnbalancedError` when Σdebit ≠ Σcredit (in integer cents) **before any insert** — zero partial writes; `InvalidPostingError` on bad shape. `apps/api/src/accounts/service.ts` (`listAccounts`, `getAccountBySubtype`) + read-only `GET /api/accounts` (auth, org-scoped). `apps/api/src/db/seed.ts` seeds the 4 accounts idempotently per `(orgId, subtype)` (1000 bank / 1200 A/R / 1499 undeposited_funds / 4000 sales_income). No migration (seed + code only). **Verified:** QA — seed x2 idempotent (psql), `GET /api/accounts` 200/401, balancing rubric 69/69 (incl. float-trap `0.30=0.10+0.20`, unbalanced→error+zero rows), and a live-Postgres probe proving `postLedger` rolls back with the caller's transaction; typecheck + `biome ci` clean; **review-approved**. Unblocks 10006/10007.
- ☑ `10006` Customer-invoice CRUD: create / edit / void a `customer_invoice` Transaction with line items; post balanced `LedgerEntry` rows (debit A/R, credit income)
  - **Delivered:** `apps/api/src/invoices/service.ts` — `createInvoice`/`getInvoice`/`listInvoices`/`updateInvoice`/`voidInvoice`, org-scoped, each in one `db.transaction` covering the `transactions`+`transaction_lines` writes, the ledger posting, and a `writeAuditLog` row. Create posts **debit A/R (total) / credit income per line** via `postLedger`, `status='open'`, `balance=total`. **Immutable ledger:** edits/voids never UPDATE/DELETE a `LedgerEntry` — `zeroOutLedger` nets debit−credit per account across all prior entries and posts the negation, then (edit) reposts; re-void is a no-op. State machine gated to `open` (edit/void on void/paid → `409`, not 500); `version` increments. Line income defaults to Sales Income; explicit `accountId` resolved org-scoped. `apps/api/src/routes/invoices.ts` — `POST`/`GET` list/`GET :id`/`PATCH`/`POST :id/void`, Fastify schemas (`additionalProperties:false`, uuid/date, `minItems:1`, qty>0, price>=0), typed errors → 400/404/409/422. Money in integer cents (round per line, sum of lines). **Verified:** QA docker-compose HTTP+psql — $100 create → balanced 2-row ledger + audit; edit → totals/version updated, old rows retained, net ties to new total; void → net 0, balance 0, record kept; double-void/edit-of-void → 409; multi-line + rounding (3dp → 400); non-customer & real cross-org contact → 422, cross-org invoice → 404; unauth → 401; `vitest` 96/96, typecheck + `biome ci` clean; **review-approved** (2 non-blocking follow-ups filed).
- ☑ `10007` Payments: record a payment `Transaction` against an invoice; post ledger (debit bank / undeposited funds, credit A/R); derive paid / partial / unpaid status
  - **Delivered:** `payment_applications` join table (migration `0004_nebulous_stature.sql`) links a payment `Transaction` to the invoice it pays by applied amount — the accountant-correct model that makes status derivation a clean sum. `apps/api/src/payments/status.ts` — pure `deriveInvoiceStatus(totalCents, paidCents)` (`open`/`partially_paid`/`paid`). `apps/api/src/payments/service.ts` — `recordPayment`/`listPaymentsForInvoice`/`getPayment`/`voidPayment`, each atomic in one `db.transaction` (payment row + application + ledger + invoice recompute + audit), org-scoped. Record posts **debit deposit (default Undeposited Funds) / credit A/R**, rejects overpayment via integer-cent guard (`422`); void reverses via the shared **`zeroOutLedger`** (extracted from invoices into `ledger/posting.ts`, no `LedgerEntry` mutated/deleted) and steps the invoice status back down. `apps/api/src/routes/payments.ts` — `POST /api/invoices/:id/payments`, `GET .../payments`, `GET /api/payments/:id`, `POST /api/payments/:id/void`; typed errors → 400/404/409/422. **Verified:** QA docker-compose HTTP+psql — partial 40 → partially_paid/60 (balanced ledger + application + audit), full 60 → paid/0, overpayment 120 → 422 nothing written, pay-a-paid → 409, void → back to partially_paid/60 (ledger net 0, rows retained, application removed, audit void), double-void → 409; cross-org isolation on all 4 endpoints → 404, non-asset deposit → 422; `deriveInvoiceStatus` boundary tests real; regression: invoice edit/void on partially_paid still 409; `vitest` 136/136, typecheck + `biome ci` clean; **review-approved** (2 non-blocking follow-ups: concurrent-payment row lock, voided payments drop from list view).
