# Done

Completed and verified tasks. Keep the original task ID.

## Original phase plan

Target deploy was end of day **Wed Jul 8** (~6 days from Thu Jul 2). All phases below
completed on schedule except the Phase-3 hardening items opened after external review
(`30020`–`30026`, tracked in `TODO.md`).

| Phase | Theme | Rough window |
|-------|-------|--------------|
| 0 | Design, local env, accounts | Thu Jul 2 |
| 1 | Core app (accounting core, auth, customer invoices) + CI | Fri Jul 3 – Sat Jul 4 |
| 2 | Sync engine (QBO, idempotency, conflicts, retries) + CD | Sun Jul 5 – Tue Jul 7 |
| 3 | Terraform infra + deploy + hardening | Tue Jul 7 – Wed Jul 8 |
| 4 | Stretch / roadmap (vendor bills, refunds, reports) | beyond the 6-day target |

## Phase 0 — Design & foundations (`0000x`)

- ☑ `00001` Architecture decisions doc (`docs/architecture-decisions.md`)
- ☑ `00002` Product requirements doc (`docs/PRD.md`)
- ☑ `00003` Project instructions (`CLAUDE.md`)
- ☑ `00004` Backlog structure (`docs/backlog/`: TODO, TOCODEREVIEW, TOTEST, DONE)
- ☑ `00005` Register QuickBooks Online developer account + sandbox company
- ☑ `00006` Create QBO app: OAuth client id/secret, redirect URIs, webhook endpoint
- ☑ `00007` AWS account + IAM for Terraform; S3 remote state backend with native locking (`use_lockfile`)
- ☑ `00008` Local dev env: `docker-compose` (postgres:17 + Node 24 app) + base tooling
  - **Delivered:** `docker-compose.yml` (Postgres 17 with healthcheck + `db-data` volume; app gated on db health, source bind-mount + anonymous `node_modules` volume for hot reload), `Dockerfile` (node:24 + Corepack/pnpm), minimal `src/index.ts` (`node:http` `/health` that pings Postgres via `pg`), `package.json` + `packageManager` pin, `tsconfig.json`, `.env.example`, `.dockerignore`, and Biome (`biome.json`, 2-space to match pnpm, `format`/`lint`/`check`/`ci` scripts). Verified: `docker compose up --build` → `GET /health` → `200 {"status":"ok","db":"up"}`; `tsc --noEmit` and `biome ci` clean.
- ☑ `00009` Repo scaffolding remainder: pnpm workspace layout + typed `.env` config loader
  - **Delivered:** `pnpm-workspace.yaml` (`apps/*`, `packages/*`); backend at `apps/api` (`@invoicing/api`) with a dependency-free typed env loader (`apps/api/src/config.ts`); shared `tsconfig.base.json` extended per package; root `package.json` as workspace root. Verified: `docker compose up --build` → `/health` `200`; `pnpm -r typecheck` + `biome ci` clean.

## Phase 1 — Core app + CI (`1000x`)

Goal: a working, locally-runnable app with auth and customer-invoice / payment CRUD
on a double-entry ledger, backed by Postgres, with CI green on every push. CI was
front-loaded (`10002`); from there each task added its own Vitest unit tests for the
pure logic it introduced, rather than a separate testing task. Once the core app
(`10003`–`10011`) landed, the `1001x` tasks adapted `apps/web` to the **Clearbook
design system** (`docs/design-system.md`) as a restyle, not a rewrite — existing
behaviour + tests stayed green, the public SSG build kept emitting exactly 3
prerendered pages, and Phase-2 sync surfaces were intentionally left out. Each was
QA'd with Playwright (visual + behavioural).

- ☑ `10001` Drizzle schema + first migration — accounting core
  - **Delivered:** `apps/api/src/db/schema.ts` — 11 tables + 8 pg enums (unified `transactions` + `transaction_lines` + `ledger_entries`, `contacts` role flags, chart-of-accounts `accounts`, entity-typed `sync_links`). Money `numeric(14,2)`, uuid PKs, org-scoped FKs, self-ref `accounts.parent_id`, cascade on lines/ledger. `drizzle.config.ts` + `drizzle/0000_young_siren.sql`; `db:generate` + programmatic `db:migrate` (`src/db/migrate.ts`). Verified: migration applies over docker network (11 tables + `drizzle.__drizzle_migrations`); `tsc --noEmit` + `biome ci` clean.
- ☑ `10002` CI (GitHub Actions) + Vitest setup
  - **Delivered:** `.github/workflows/ci.yml` (push to `main` + PRs): install → `biome ci` → `pnpm -r typecheck` → `pnpm -r test` → `docker build`. Vitest wired (`apps/api` + root `test` scripts, `vitest.config.ts`); `config.ts` refactored to testable `loadConfig(env)` + `config.test.ts` (4 cases). Verified: `vitest` 4/4, `biome ci`/typecheck clean, **CI green on GitHub**.
- ☑ `10003` Fastify server bootstrap
  - **Delivered:** replaced the `node:http` placeholder with Fastify. `src/app.ts` (`buildApp`, injectable pool for tests), `src/plugins/db.ts` (fastify-plugin decorating `pool` (pg) + `db` (drizzle), owns pool lifecycle via `onClose`), `src/routes/health.ts` (`GET /health` pings the pool), `src/index.ts` (listen on `0.0.0.0:config.port`, graceful SIGTERM/SIGINT). Pino structured logging (pretty in dev via pino-pretty, JSON otherwise). Unit tests `src/routes/health.test.ts` (200 up / 503 down via `app.inject` + fake pool). **Verified:** `docker compose up --build` → `GET /health` `200`; `vitest` 6/6, typecheck + `biome ci` clean; **CI green, merged to `main` via PR #8**.
- ☑ `10004` Auth: email/password session login, httpOnly cookie, logout, seeded users, Admin/Member roles
  - **Delivered:** DB-backed opaque-token sessions (`sessions` table + migration `0001_huge_newton_destine.sql`). `apps/api/src/auth/password.ts` (scrypt N=16384/r=8/p=1, `timingSafeEqual`, never throws on malformed hash), `auth/session.ts` (256-bit token, only SHA-256 hash stored, create/find/delete, expiry enforced). `plugins/auth.ts` (`@fastify/cookie` signed with `SESSION_SECRET`; `authenticate` + `requireRole` preHandlers; cookie `httpOnly`/`sameSite=lax`/`secure`-in-prod). `routes/auth.ts` (`POST /api/auth/login` — Fastify JSON schema, dummy-hash timing defense against user-enumeration, generic `401`; `POST /api/auth/logout` — real server-side revocation, idempotent; `GET /api/auth/me`). `db/seed.ts` (idempotent org + admin/member users, `db:seed`). `config.ts`/`.env.example`/`docker-compose.yml` wired for `SESSION_SECRET`/`SESSION_TTL_HOURS`/`SEED_*_PASSWORD`; `plugins/db.ts`/`app.ts` accept an injectable `db` for route tests. **Verified:** QA ran the full containerized `docker compose up --build` flow (boots, `/health` 200, seed x2 idempotent, login→`/me`→logout→`/me` 200/200/204/401 with session revoked); `vitest` 28/28, typecheck + `biome ci` clean; **review-approved** (2 non-blocking follow-ups filed). Delivered across 2 attempts (attempt-1 QA reject: compose env passthrough).
- ☑ `10005` Contact CRUD (customer role first): name + contact info, attachable to an invoice; maps to a QBO Customer
  - **Delivered:** org-scoped contact CRUD behind `app.authenticate` (both roles). `apps/api/src/contacts/service.ts` (`createContact`/`listContacts`/`getContact`/`updateContact`/`archiveContact`, every query filtered by `orgId`); `apps/api/src/routes/contacts.ts` (`POST`/`GET` list/`GET :id`/`PATCH`/`DELETE` with JSON schemas — uuid params, email format, `minLength`, PATCH `minProperties:1`, `additionalProperties:false`). Soft-archive via a new `is_active` column (migration `0002_married_cassandra_nova.sql`), QBO-style deactivate-not-delete; archive is idempotent, list excludes archived unless `?includeInactive=true`. Also disabled Fastify's default `removeAdditional` (`ajv.customOptions.removeAdditional:false` in `app.ts`) so `additionalProperties:false` truly 400s — an app-wide fix that also tightens the 10004 login schema. No QBO `SyncLink` / audit writes (deferred to Phase 2 / `10008`). **Verified:** QA ran full CRUD + validation + idempotent-archive against the docker-compose stack as both admin & member, proved cross-org isolation with a genuine second org (GET/PATCH/DELETE/LIST all 404/exclude), and regression-checked the `removeAdditional` change (login intact, now 400s on unknown field); `vitest` 45/45, typecheck + `biome ci` clean; **review-approved** (1 non-blocking nit: whitespace-only `displayName` trims to empty).
- ☑ `10008` Audit log write path: every mutating action appends to `SyncAuditLog` (entity, action, direction, outcome, user, timestamp)
  - **Delivered:** append-only audit on every local mutation, atomic with the change. Added `'local'` to the `sync_direction` enum (migration `0003_gorgeous_wolf_cub.sql`) so app-originated actions are logged distinctly from inbound/outbound QBO sync. New `apps/api/src/audit/service.ts` (`writeAuditLog(tx, entry)` + `AuditEntry`, defaults `direction:'local'`/`outcome:'success'`). `contacts/service.ts` create/update/archive refactored to `db.transaction(tx => …)` writing one audit row each on the same `tx` (attributed to `user.id`/`orgId` from the session — not spoofable); `routes/contacts.ts` passes `user.id`. Reads and no-row-matched no-ops write nothing; `detail` carries only changed field names. Tests: `audit/service.test.ts` + `contacts.test.ts` (one row per mutation, reads/404s write nothing, forced-failure rollback leaves neither contact nor audit row). **Verified:** QA on fresh `docker compose up --build` (migration 0003 applied, `enum_range`=inbound/outbound/local) psql-confirmed one correct row per create/update/archive, cross-user attribution (member vs admin), and zero rows from reads/no-ops; `vitest` 48/48, typecheck + `biome ci` clean; **review-approved**.
- ☑ `10011` Chart of accounts: seed minimal accounts (A/R, Sales Income, bank, Undeposited Funds) + balanced posting helper that rejects unbalanced transactions
  - **Delivered:** the ledger core. `apps/api/src/money.ts` — exact integer-cents `toCents`/`formatCents` (regex-parsed, no float path; rejects `>2` decimals / malformed / non-finite). `apps/api/src/ledger/posting.ts` — `postLedger(tx, {orgId, transactionId, entryDate, lines})`, transaction-aware (posts inside the caller's txn, no HTTP surface), validates each line (no negative / both-sides / both-zero / empty set) and throws `UnbalancedError` when Σdebit ≠ Σcredit (in integer cents) **before any insert** — zero partial writes; `InvalidPostingError` on bad shape. `apps/api/src/accounts/service.ts` (`listAccounts`, `getAccountBySubtype`) + read-only `GET /api/accounts` (auth, org-scoped). `apps/api/src/db/seed.ts` seeds the 4 accounts idempotently per `(orgId, subtype)` (1000 bank / 1200 A/R / 1499 undeposited_funds / 4000 sales_income). No migration (seed + code only). **Verified:** QA — seed x2 idempotent (psql), `GET /api/accounts` 200/401, balancing rubric 69/69 (incl. float-trap `0.30=0.10+0.20`, unbalanced→error+zero rows), and a live-Postgres probe proving `postLedger` rolls back with the caller's transaction; typecheck + `biome ci` clean; **review-approved**. Unblocks 10006/10007.
- ☑ `10006` Customer-invoice CRUD: create / edit / void a `customer_invoice` Transaction with line items; post balanced `LedgerEntry` rows (debit A/R, credit income)
  - **Delivered:** `apps/api/src/invoices/service.ts` — `createInvoice`/`getInvoice`/`listInvoices`/`updateInvoice`/`voidInvoice`, org-scoped, each in one `db.transaction` covering the `transactions`+`transaction_lines` writes, the ledger posting, and a `writeAuditLog` row. Create posts **debit A/R (total) / credit income per line** via `postLedger`, `status='open'`, `balance=total`. **Immutable ledger:** edits/voids never UPDATE/DELETE a `LedgerEntry` — `zeroOutLedger` nets debit−credit per account across all prior entries and posts the negation, then (edit) reposts; re-void is a no-op. State machine gated to `open` (edit/void on void/paid → `409`, not 500); `version` increments. Line income defaults to Sales Income; explicit `accountId` resolved org-scoped. `apps/api/src/routes/invoices.ts` — `POST`/`GET` list/`GET :id`/`PATCH`/`POST :id/void`, Fastify schemas (`additionalProperties:false`, uuid/date, `minItems:1`, qty>0, price>=0), typed errors → 400/404/409/422. Money in integer cents (round per line, sum of lines). **Verified:** QA docker-compose HTTP+psql — $100 create → balanced 2-row ledger + audit; edit → totals/version updated, old rows retained, net ties to new total; void → net 0, balance 0, record kept; double-void/edit-of-void → 409; multi-line + rounding (3dp → 400); non-customer & real cross-org contact → 422, cross-org invoice → 404; unauth → 401; `vitest` 96/96, typecheck + `biome ci` clean; **review-approved** (2 non-blocking follow-ups filed).
- ☑ `10007` Payments: record a payment `Transaction` against an invoice; post ledger (debit bank / undeposited funds, credit A/R); derive paid / partial / unpaid status
  - **Delivered:** `payment_applications` join table (migration `0004_nebulous_stature.sql`) links a payment `Transaction` to the invoice it pays by applied amount — the accountant-correct model that makes status derivation a clean sum. `apps/api/src/payments/status.ts` — pure `deriveInvoiceStatus(totalCents, paidCents)` (`open`/`partially_paid`/`paid`). `apps/api/src/payments/service.ts` — `recordPayment`/`listPaymentsForInvoice`/`getPayment`/`voidPayment`, each atomic in one `db.transaction` (payment row + application + ledger + invoice recompute + audit), org-scoped. Record posts **debit deposit (default Undeposited Funds) / credit A/R**, rejects overpayment via integer-cent guard (`422`); void reverses via the shared **`zeroOutLedger`** (extracted from invoices into `ledger/posting.ts`, no `LedgerEntry` mutated/deleted) and steps the invoice status back down. `apps/api/src/routes/payments.ts` — `POST /api/invoices/:id/payments`, `GET .../payments`, `GET /api/payments/:id`, `POST /api/payments/:id/void`; typed errors → 400/404/409/422. **Verified:** QA docker-compose HTTP+psql — partial 40 → partially_paid/60 (balanced ledger + application + audit), full 60 → paid/0, overpayment 120 → 422 nothing written, pay-a-paid → 409, void → back to partially_paid/60 (ledger net 0, rows retained, application removed, audit void), double-void → 409; cross-org isolation on all 4 endpoints → 404, non-asset deposit → 422; `deriveInvoiceStatus` boundary tests real; regression: invoice edit/void on partially_paid still 409; `vitest` 136/136, typecheck + `biome ci` clean; **review-approved** (2 non-blocking follow-ups: concurrent-payment row lock, voided payments drop from list view).
- ☑ `10009` Frontend scaffold: React/Vite single app — public SSG routes (`/`, `/products`, `/pricing`) + client-rendered auth routes (`/login`, `/dashboard`) against the real `/api/auth/*`
  - **Delivered:** `apps/web` (`@invoicing/web`), Vite 8 + React 19 + react-router-dom 7 in the pnpm workspace. Public marketing pages (`src/routes/{Home,Products,Pricing}.tsx`) prerender to static HTML; auth routes (`src/routes/{Login,Dashboard}.tsx`, plus `Invoices.tsx` — a `10010` placeholder) are client-rendered behind `src/lib/RequireAuth.tsx` (loader while resolving, `<Navigate to="/login" replace>` on 401, passes the resolved user down via render-prop so `Dashboard` doesn't refetch). Same-origin httpOnly-cookie model: `src/lib/api.ts` (`login`/`logout`/`me`, `credentials:'include'`) + `src/lib/session.ts`; the frontend never reads the cookie. Dev proxy `/api → API_PROXY_TARGET` (default `http://localhost:8080`) in `vite.config.ts`. **SSG deviation (plan-authorized):** `vite-ssg` is Vue-only, so used a `vite build` + `--ssr src/entry-server.tsx` + `scripts/prerender.mjs` fallback that renders each public route via `react-dom/server` + `StaticRouter` and splices it into the client `index.html` (emits `dist/{,products/,pricing/}index.html`, deletes the intermediate `dist-ssr`). Added the `web` docker-compose service (port 5173, `API_PROXY_TARGET=http://app:8080`), a `COPY apps/web/package.json` in the root `Dockerfile`, and a web vitest+JUnit step in CI's `unit-tests` job. **QA found + fixed one real bug (surfaced only by the real browser):** `api.ts` sent `Content-Type: application/json` on the bodyless `logout()`, tripping Fastify's `FST_ERR_CTP_EMPTY_JSON_BODY` (400) so the session was never cleared — fixed client-side to send the JSON header only when a body is present (general, not a logout special-case), guarded by a red-before-green regression test (`api.test.ts`). **Verified:** HTTP QA (gates, distinct SSG artifacts, real login→me→logout→401 for both roles) **and** a real-browser Playwright pass (public pages network-free, no protected-content flash, `document.cookie` confirms httpOnly, logout now 204 + session row deleted + stays on `/login` + back-button re-guard, member login works); `vitest` 146/146 (api 136 + web 10), typecheck + `biome ci` clean, `docker build` clean; **review-approved** (attempt 2; 4 cosmetic non-blocking follow-ups filed).
- ☑ `10010` Dashboard + invoice list/detail UI with inline sync status badge (synced / pending / conflict / failed)
  - **Delivered:** the real product UI on the 10009 scaffold, consuming the 10005–10007 endpoints (server stays source of truth). **Backend (one small change):** `apps/api/src/invoices/service.ts` `getInvoice`/`listInvoices` LEFT JOIN the existing `sync_links` table (predicate bound to `transactions.orgId` + `entity_type='transaction'` + `local_id=transactions.id`, `COALESCE`d to `'pending'`) and expose `syncState` on the invoice; serialized in `routes/invoices.ts`. The `(orgId, entityType, localId)` UNIQUE constraint keeps it a single non-fanning query (no N+1), org-scoped so no cross-org leak. No migration (table + `sync_state` enum already existed) — Phase 2 sync populates data, not UI. **Frontend (`apps/web`):** extended `lib/api.ts` with typed invoice/payment/contact/account methods + `lib/money.ts`; new `components/{SyncStatusBadge,InvoiceStatusBadge,InvoiceLinesEditor,RecordPaymentDialog}.tsx`; new routes `routes/{Invoices,InvoiceDetail,InvoiceNew,InvoiceEdit,Customers,Dashboard}.tsx` (Invoices/Dashboard rewritten from the 10009 placeholders) under `RequireAuth` in `App.tsx` with a shared authed nav. Edit/Void hidden unless `status==='open'` (with graceful 409-race handling); record-payment overpayment → inline 422 with nothing written; money display-only via `formatMoney` (Dashboard totals accumulate in integer cents). **Verified:** `vitest` **190/190** (api 140 + web 50), typecheck + `biome ci` + `docker build` clean, `pnpm --filter @invoicing/web build` still emits exactly the 3 prerendered public pages. QA drove all 10 plan scenarios in a real browser (Playwright) — full invoice lifecycle (open→partially_paid→paid), void invoice + void payment, overpayment 422, empty/not-found states, member role — with `psql` confirming balanced ledger at every step, **and independently verified `syncState` against real Postgres** (pending→synced flip, `conflict`/`failed` distinct, cross-org non-leak) to cover a fake-DB-harness `.leftJoin()` deviation; **review-approved** (1 non-blocking follow-up: the test harness hardcodes the join predicate — keep the real-PG cross-org check / add a pglite harness when Phase 2 lands). Closes Phase 1.
- ☑ `10012` Design system foundation + app shell (Clearbook) — first task of the Phase-1 UI restyle
  - **Delivered:** the Clearbook design-system foundation that `10013`–`10017` build on. Self-hosted **IBM Plex Sans/Mono** (`@fontsource/*` latin subsets, imported only in `main.tsx` so the SSG public pages stay self-contained — zero external font requests). Design **tokens** in `apps/web/src/theme.ts` + `:root` vars in `apps/web/src/styles/global.css` (base/reset/custom scrollbar, plus `.ui-*` interactive-state classes that own hover/focus so an inline style can't shadow a stateful rule). Reusable **primitives** in `apps/web/src/components/ui/` (`Button` incl. `ghost`, `Card`, `Input`/`Select`/`Textarea`, `FieldLabel`, `PageHeader`, `DataState`). The **app shell** in `apps/web/src/components/shell/` (238px sidebar: brand mark+wordmark, MENU icon-nav with active pill + live Invoices count + disabled COMING SOON group + user chip w/ logout; 60px topbar: page title + search + New invoice) wired into `App.tsx`, replacing the old `AuthedLayout` — auth is now guarded **once** at the shell and `user` flows via `Outlet` context (removes a double `/api/auth/me`/double-guard). Restyled `InvoiceStatusBadge` + `SyncStatusBadge` (all 4 states; sync labels aligned to Pending/Synced/Conflict/Failed) keeping props/exports stable. **Phase-1 scope guards honoured:** topbar shows neutral "Not connected" (no fabricated QBO status); `Integrations` routes to a placeholder; no "Save & sync" introduced. **Verified:** `vitest` 190/190 (api 140 + web 50), typecheck + `biome ci` + `docker build` clean, `pnpm --filter @invoicing/web build` still emits exactly the 3 prerendered public pages; Playwright QA confirmed the shell/nav/user-chip/logout(204)+re-guard, self-hosted fonts, badges, and a strong visual match to the comp; **review-approved** (primitives called "exemplary"), with 4 small foundation follow-ups fixed before merge (dead-hover logout, search focus ring, spec-aligned sync labels, tokenized `#9aa79f`/`#f0cfc9`).
- ☑ `10013` Auth + public marketing restyle (Login + Home/Products/Pricing) to Clearbook
  - **Delivered:** the auth + public marketing surface restyled onto the 10012 foundation. New `apps/web/src/components/marketing/PublicLayout.tsx` — restyled public chrome (sticky top nav: Clearbook `Logo` + Products/Pricing links + `Sign in` CTA; footer) wrapping `/`, `/products`, `/pricing`, plus `CtaLink` (a link-styled-as-button that reuses the shared `.ui-btn` classes and stays a real `<Link>` — right element for a route change) and `MarketingSection` (centered ~1040px content column). `routes/Home.tsx` — hero (Clearbook eyebrow pill + "The invoicing platform that stays in sync" + primary/secondary CTAs) + 3 feature `Card`s. `routes/Products.tsx` — 3 product `Card`s. `routes/Pricing.tsx` — single "Standard $29/mo" pricing `Card` (mono numerals, checkmark feature list, CTA). `routes/Login.tsx` — **standalone centered branded auth card** (`Logo` + `Card` + `Input` + `Button`, inline `role="alert"` error); the `me()` already-signed-in skip, `login()`, `navigate('/dashboard')`, submit-state, and generic-error behaviour are preserved verbatim. `App.tsx` wraps the marketing routes in `PublicLayout` and renders `/login` standalone (no marketing chrome). **Phase-1 scope guards honoured:** no fabricated QBO/sync status; marketing copy describes the product as before. **Verified:** `vitest` 50/50 web (all accessible-name contracts preserved; `Pricing.test` wrapped in `MemoryRouter` like `Home.test` since the page now links — assertion unchanged), `tsc --noEmit` + `biome ci` clean, SSG build still emits **exactly the 3** prerendered pages (`/`, `/products`, `/pricing`) with the restyled content present in the static HTML and no network calls (`Login` is client-only, not prerendered); CI green (unit-tests + verify). **Live Playwright visual QA** (installed in-session) drove all four routes at 1280px + the login error path (bad creds → API down → red `role="alert"` "Invalid email or password.", values retained) — clean, strong Clearbook match; self code-review. Squash-merged as `7e909a4` (#22). Remaining restyle queue: 10014-10017.
- ☑ `10014` Dashboard restyle (greeting, KPI cards, recent invoices, sync-health) to Clearbook
  - **Delivered:** `apps/web/src/routes/Dashboard.tsx` restyled onto the 10012 primitives/shell. Greeting header ("Welcome back, <name>" + email + role; the topbar already renders the "Dashboard" title). Responsive KPI `StatTile` row (local component to the design-system "Stat/KPI tile" spec — colored dot + muted uppercase label + big mono value + colored sub): **Outstanding A/R** (brand-green mono + "N unpaid invoices"), **Open** / **Partially paid** / **Paid** counts. **Recent invoices** `Card` — up to 5 rows (mono doc number, right-aligned `tabular-nums` total, `InvoiceStatusBadge`), `.ui-table-row` hover, `View all` → /invoices, empty state ("Create your first invoice"). **Sync health** `Card` — all four `SyncStatusBadge` states with **real counts** reduced from each invoice's `syncState` (Phase-1 conflict/failed come out `0` from the data, not hardcoded), plus an honest "Not connected to QuickBooks yet — sync starts in a later phase" note → Integrations (**no fabricated connected status**, per the Phase-1 scope guards). Client-derived aggregation + `LoadingState`/`ErrorState` (`role=status`/`role=alert`) semantics preserved; **no API change** (reads the 10010 `listInvoices` + `syncState`). **Verified:** web `vitest` 50/50, `tsc` + `biome ci` clean; SSG unchanged — Dashboard is auth-gated, not prerendered — still exactly the 3 public pages; CI green. **Live Playwright visual QA** (route-stubbed `/api/auth/me` + `/api/invoices`, since it's auth-gated): populated (Outstanding A/R $2,525.50 = 1450+400+675.50; Open 2 / Partial 1 / Paid 2; 3 unpaid; sync Pending 5 / Synced 1 / Conflict 0 / Failed 0) + empty ($0.00, zeros, create-first-invoice) both render correctly inside the shell. Squash-merged as `cb3e79c` (#23). Remaining restyle queue: 10015-10017.
- ☑ `10015` Invoices list + create/edit restyle (table, filters, line-editor, sticky summary) to Clearbook
  - **Delivered:** the invoices list + create/edit surface restyled onto the 10012 primitives/shell (restyle only, **no API change**). `routes/Invoices.tsx` — filter tabs (All/Open/Partially paid/Paid/Void with live counts + active pill) + a `Card`-wrapped **CSS-grid table** (`surface-muted` faint-uppercase header; per-row mono doc#, customer, mono date, right-aligned `tabular-nums` total + balance, `InvoiceStatusBadge` + `SyncStatusBadge`, trailing chevron, `.ui-table-row` hover, whole row → detail), a per-filter empty message + the zero-invoices `EmptyState`; load/error via `LoadingState`/`ErrorState`. `components/InvoiceLinesEditor.tsx` — aligned grid line rows (Description/Qty/Unit price/**Amount** live per line + `×` remove) via the `Input` primitive (qty/price `mono`) + a "+ Add line" `Button`; **all exports + the `Line N …` aria-labels preserved** (old inline total moved into the page Summary). New `components/InvoiceSummary.tsx` — sticky right-hand **Summary** panel (Subtotal + Total from `computeDraftTotal`, inline `role=alert` error slot, submit-button slot). `routes/InvoiceNew.tsx` + `routes/InvoiceEdit.tsx` — two-column layout (Details `Card` + Line items `Card` on the left, `InvoiceSummary` on the right) via `PageHeader`/`Select`/`Input`/`Button`; New keeps the add-customer prompt + customer `<option>`s + "Create invoice" (disabled with no customers), Edit keeps the load/not-found/non-open guards + "Save changes". **Scope guard:** no "Save & sync" (never existed — labels stay "Create invoice"/"Save changes") and the New subtitle drops the sync clause ("Post a customer invoice to the ledger"). **Verified:** web `vitest` 50/50 (Invoices + InvoiceNew contracts — customer name, `$100.00`/`$60.00`, badges, empty/error, option names, line aria-labels, disabled submit, 422 inline — intact), `tsc` + `biome ci` clean; SSG unchanged — auth-gated, not prerendered — still exactly 3 public pages; CI green. **Live Playwright visual QA** (route-stubbed auth + contacts + invoices): list (populated + `Open`-filtered + empty), New (live Amount $1,000 = 10×100, Summary tracking), Edit (2 lines → Total $1,450) all correct inside the shell. Squash-merged as `8c64ed9` (#24). Remaining restyle queue: 10016-10017.
- ☑ `10016` Invoice detail + record-payment restyle (document card, payments, sync card) to Clearbook
  - **Delivered:** `routes/InvoiceDetail.tsx` + `components/RecordPaymentDialog.tsx` restyled onto the 10012 primitives/shell (restyle only, **no API change**). Detail: back link + header (mono "Invoice INV-xxxx" + `InvoiceStatusBadge` + date/due) with the same **gated actions** (Record payment primary / Edit secondary / Void danger — Edit+Void only when `open`, Record payment when `open|partially_paid`); two-column layout — left = the **document `Card`** (BILL TO customer + email, grid line-items table, then Subtotal / Total / **Paid** (= total − balance) / **Balance** totals) + a **Payments `Card`** (mono amount + date + status pill + `Void` action per row, or empty state) — right = a **Sync-status `Card`** (Phase-1 `SyncStatusBadge` + honest "not synced yet" note → Integrations, **no resolve/retry**). Void-invoice / void-payment / `actionError` (`role=alert`) + load/not-found/error states preserved. Dialog: proper **modal** (fixed `rgba(20,35,28,.28)` overlay + centered `Card` + `Input`/`Select`/`Button`) styling **only the API-backed fields** (amount `mono` prefilled to balance, date, deposit account defaulting to Undeposited Funds, memo) with the overpayment-**422 inline**; `role="dialog"`/`aria-modal`/labels/`<option>`s/`role=alert` all preserved (backdrop-click-to-close dropped — a11y lint; Cancel still closes). **Ledger-postings card DEFERRED per user directive** — no `GET /api/invoices/:id/ledger` endpoint, no API change, no faked debit/credit rows. No "Save & sync" strings. **Verified:** web `vitest` 50/50 (RecordPaymentDialog contract — amount prefill/`toHaveValue(60)`, `{type:'asset'}` + option name, record→`onRecorded`, 422 alert + dialog stays — intact), `tsc` + `biome ci` clean; SSG unchanged — auth-gated, still exactly 3 public pages; CI green. **Live Playwright visual QA** (route-stubbed): partially_paid detail (Paid $450 / Balance $1,000, payment row + Void, sync card), the record-payment modal (amount prefilled $1,000), and the open-invoice variant (Edit + Void, empty payments) all correct in the shell. Squash-merged as `f5db7ba` (#25). Remaining restyle queue: **10017 (Customers) — the last**.
- ☑ `10017` Customers restyle (table + add/edit slide-over drawer) to Clearbook
  - **Delivered:** `routes/Customers.tsx` restyled onto the 10012 primitives/shell (restyle only, **no API change**). `PageHeader` "Customers" + an **Add customer** primary button; a `Card`-wrapped CSS-grid **table** — per-row **avatar initials** chip (brand-tint), name + email/phone, **Invoices** count + outstanding **Balance** (aggregated per `contactId` from `listInvoices()`, no new endpoint — same approach as the dashboard), a Phase-1 `SyncStatusBadge state="pending"` (contacts have no `syncState` field yet; honest fixed Phase-1 state, consistent with every other screen), and an **Archive** action (real `archiveContact` delete-equivalent); `LoadingState`/`ErrorState`/`EmptyState`. **Add-customer right-side slide-over drawer** (fixed `rgba(20,35,28,.28)` overlay, 400px white panel, `shadow.drawer`, Name/Email/Phone via `Input`, Cancel/Add-customer footer, `role="dialog"`/`aria-modal`) wired for create via existing `createContact` (now also passing the API-supported `phone`). **Scope decisions (style what exists):** comp's **city** field omitted (not persisted); **row-level edit DEFERRED** — no `updateContact`/`PATCH /api/contacts/:id` endpoint exists and adding one is a backend change beyond the restyle (same call as the deferred ledger card), so Archive is the row action + the drawer is create-only. **Verified:** web `vitest` 50/50, `tsc` + `biome ci` clean, SSG unchanged — auth-gated, still exactly 3 public pages; CI green. **Live Playwright visual QA** (route-stubbed): table (avatar chips; Acme 2/$2,125.50, Globex 1/$400, Initech 0/$0; Pending badges; Archive), the right-side Add-customer drawer (Name/Email/Phone filled), and the empty state all correct in the shell. Squash-merged as `f6fa402` (#26).
  - **✅ Clearbook restyle sub-phase (10012–10017) COMPLETE** — all six tasks merged to main. `apps/web` now fully on the Clearbook design system (tokens, self-hosted fonts, shell, primitives; auth + marketing, dashboard, invoices list/create/edit, invoice detail + record-payment, customers). Deferred follow-ups worth a small future task: (1) `GET /api/invoices/:id/ledger` + the invoice-detail ledger-postings card; (2) `PATCH /api/contacts/:id` + wiring the customer drawer for edit. Next up: **Phase 2 — QBO sync engine + CD (2000x)**.
- ☑ `10018` Invoice-detail ledger-postings card (deferred Phase-1 UI completion)
  - **Delivered:** read-only, org-scoped `GET /api/invoices/:id/ledger` over the existing `ledger_entries` + a "Ledger postings" debit/credit card on the invoice detail page (Clearbook comp, after Payments). **No posting-logic, schema, or migration change.** `getInvoiceLedger(db, orgId, invoiceId)` in `apps/api/src/invoices/service.ts` calls `getInvoice` first (org/existence/soft-delete → `NotFoundError`), then selects `ledger_entries` inner-joined `accounts`, ordered `entryDate, createdAt, id`, totals summed in integer cents via `toCents`/`formatCents` (money columns are strings). `GET /api/invoices/:id/ledger` in `routes/invoices.ts` mirrors the `GET /:id` auth/error shape (401/404). FE: `getInvoiceLedger` + `LedgerPosting`/`InvoiceLedger` types in `apps/web/src/lib/api.ts`; the card on `InvoiceDetail.tsx` loads **non-fatally** (never blocks the page), omitted on failure/empty, with a balanced totals row. Tests: `invoices-ledger.test.ts` (8, real pglite — balanced postings, entryDate ordering across an edit, void nets to zero, cross-org 404, soft-deleted 404, unauth 401, nonexistent 404, and a payment's own postings **not** leaking in) + `InvoiceDetail.test.tsx` (+3). api 476→484 (+8), web 71→74 (+3). **QA passed** attempt 1 — live Playwright E2E on a real docker-compose stack; cents-sum mutation spot-checks bit; confirmed a payment's postings don't appear in the invoice ledger (they post against the payment's own `transactionId`, so the plan's §2-step-3 scenario was the plan's error, not the code). **Review-approved** (read-only, org-scoped, deterministic ordering, non-fatal FE; the ledger query's `orgId` filter is defense-in-depth since `getInvoice` gates first). Squash-merged as `0e0fbdd` (#43); the same PR also carried a `CLAUDE.md` overhaul (pre-implementation stub → current platform state) per a user request.
- ☑ `10019` Customer edit (deferred Phase-1 UI completion)
  - **Delivered:** frontend-only — the `PATCH /api/contacts/:id` endpoint + org-scoped, audit-logged `updateContact` service already existed from task `10005`; this task only wired the client. New `updateContact(id, input)` + `UpdateContactInput` type in `apps/web/src/lib/api.ts` (mirrors `createContact`). `apps/web/src/routes/Customers.tsx`'s existing add-drawer now serves both create and edit: new `editingId` state, `openEditDrawer(customer)` prefills displayName/email/phone, a new "Edit" row action next to "Archive" (actions grid column widened 90px→150px, both header and rows share the one `GRID_COLUMNS` constant so they stay aligned), `handleSubmit` (renamed from `handleCreate`) branches create vs. update, and the drawer title/dialog `aria-label`/submit-button label all swap on `editingId`. New `apps/web/src/routes/Customers.test.tsx` (5 tests: edit prefill, submit calls `updateContact` and reloads the list, cancel calls neither API, the create path stays regression-free, and an update rejection shows an inline error while keeping the drawer + entered values). web 74→79 (+5). **QA passed** — live Playwright E2E against a real docker-compose stack + Postgres: edit prefill, save persists across a full page reload (verified in Postgres), cancel mutates nothing, edit→cancel→add-customer opens a blank form (no leftover state), and a route-intercepted 500 shows the inline error with the drawer staying open and the row unmutated (verified in Postgres). **Review-approved** (no scope drift — confirmed zero `apps/api` files touched, no migration; `editingId` lifecycle correct in every path; no `dangerouslySetInnerHTML`/XSS surface introduced; grid-column widening doesn't break header/row alignment since both share `GRID_COLUMNS`; tests non-tautological).

## Phase 2 — Sync engine + CD (`2000x`)

Goal: real two-way sync against the QBO sandbox, safe under duplicate / out-of-order
events and partial failures, plus continuous deploy on merge to main. Two small
Phase-1 UI features (`10018`, `10019`) were carved out of the Clearbook restyle and
deferred here because each needed a small new backend endpoint — both landed later
(`10018` alongside this phase's work, `10019` after the Phase-3 GCP migration).

- ☑ `20001` QBO OAuth: connect/disconnect flow, token storage in `QboConnection`, automatic token refresh
  - **Delivered:** the Phase-2 foundation — the QBO OAuth connection lifecycle plus the reusable seams every later sync task (20002–20011) builds on. **Injectable `QboOAuthClient`** (`apps/api/src/qbo/oauth-client.ts`) — real Intuit OAuth2 HTTP client (Basic-auth token exchange / refresh / revoke, `application/x-www-form-urlencoded`, `redirect_uri` on code-exchange but not on refresh) behind an interface so tests inject a stub (no live Intuit in CI). **`getValidAccessToken(db, client, orgId)`** (`connection-service.ts`) — refresh-on-demand primitive: 60s expiry skew, persists rotated tokens, throws `QboNotConnectedError` leaving no half-updated row when refresh fails. **Stateless HMAC-signed `state`** (`oauth-state.ts`, `timingSafeEqual`, `SESSION_SECRET`, 10-min window, tamper/foreign-org rejection). **Admin-gated routes** `/api/integrations/qbo/{connect,callback,status,disconnect}` (`routes/integrations.ts`): connect returns the Intuit authorize URL (503 `qbo_not_configured` when unset); callback verifies `state` + cross-checks `orgId` → exchanges code → upserts the single per-org `qbo_connections` row → 302 to fixed relative `/integrations?connected=1|?error=…` (no open-redirect); status never serializes tokens; disconnect best-effort-revokes then deletes, idempotent. Optional/nullable `config.qbo` (never throws on missing `QUICKBOOKS_*`); new `plugins/qbo.ts` decorating `app.qboOAuthClient`, injectable via `BuildAppOptions`. Env `QUICKBOOKS_REDIRECT_URI`/`QUICKBOOKS_ENVIRONMENT` in `.env.example` + `docker-compose.yml`; `## QBO OAuth` note in `docs/design-decisions.md`. No schema migration (`qbo_connections`/`sync_audit_logs` already existed). The deploy-time QBO **client secret** will be injected from **AWS SSM Parameter Store** (Phase 3 / 20014); per-org OAuth tokens stay in the app DB. **Verified:** `pnpm -r typecheck` clean, `pnpm -r test` 186 api + 50 web, `biome ci` + `docker build` clean, web build emits exactly the 3 prerendered pages. QA verified non-tautological tests (token-request shape, state tamper/expiry/foreign-org, refresh-vs-stored + no-half-updated-row on refresh-throw, full HTTP matrix 401/403/200/503/302/400 `invalid_state`/idempotent disconnect, `/status` no-token-leak) + Playwright admin `status` 200 `{connected:false}` / member 403; **review-approved** (2 non-blocking follow-ups: concurrent-refresh single-flight for 20005/20006; upsert atomicity fine beyond single-admin connect). Live Intuit sandbox OAuth is user-gated (phase-end). Squash-merged as `b143d4c` (#27).
- ☑ `20002` Webhook ingestion endpoint: JSON-schema validation of inbound QBO payloads (Fastify schema), signature verification
  - **Delivered:** the inbound edge — public `POST /api/integrations/qbo/webhook` (no session; the signature is the auth), strictly receive → verify → validate → resolve realm→org → record receipt → 200 (no `SyncLink`/`Transaction` writes; refetch/dedup/apply are 20003/20005/20007). **Signature verification** (`apps/api/src/qbo/webhook-signature.ts`) — constant-time `timingSafeEqual` base64 HMAC-SHA256 over the **raw** request bytes vs `intuit-signature`. Verified via an **encapsulated Fastify content-type parser** registered only inside `apps/api/src/routes/qbo-webhook.ts` (parser + `setErrorHandler` scoped to the child plugin instance, so the global JSON parser every other route uses is untouched — locked by a `POST /api/auth/login` regression test). **Fails closed / acks fast:** no verifier token → `503 qbo_webhook_not_configured`; missing/bad signature → `401 invalid_signature`; malformed JSON → `400 invalid_json`; wrong shape → `400` via `webhookBodySchema` (`webhook-types.ts`); unknown `realmId` (via new `getConnectionByRealmId`) → `200` acked + `log.warn` with **no** audit row (anti-retry-storm); known realm → `200 {ok:true}` + one `sync_audit_logs` receipt row per entity (`direction:'inbound'`, `action:'qbo.webhook.received'`, QBO id in `triggeringEvent`/`detail`). New `QUICKBOOKS_WEBHOOK_VERIFIER_TOKEN` → nullable `config.qbo.webhookVerifierToken`, injectable via `BuildAppOptions.qboWebhookVerifierToken`/`qboPlugin` so tests sign with a known token. No schema migration; `docs/design-decisions.md` extended. **QA caught + fixed two real bugs the mocked test harness couldn't (both live-Postgres verified):** (1) `QboWebhookError` used a TS **parameter property** → Node strip-only TS crash-looped the whole API at boot (`ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX`) — rewritten to a plain field; a mandatory `docker compose up` + `/health` 200 boot check is now part of verification. (2) `local_id` (a `uuid` column) was fed Intuit's non-UUID entity id → 500 on the success path — now `localId: null` for QBO-originated events (`AuditEntry.localId` widened to `string | null`; QBO id preserved in `triggeringEvent`/`detail`). **Verified:** `pnpm -r typecheck` clean, `pnpm -r test` 205 api + 50 web, `biome ci` + `docker build` clean, web build 3 pages; live proof — correctly-signed webhook (entity id `"145"`) → 200 with psql showing exactly one audit row `local_id IS NULL`, 503/401 hold, `/api/auth/login` unaffected on the same running instance; **review-approved** (attempt 3). Non-blocking follow-ups: verifier-token/config coupling (hoist to top-level config if the webhook edge ever needs to be live before OAuth creds); per-request idempotency deferred to 20005. Live Intuit sandbox webhook delivery is user-gated (phase-end). Squash-merged as `3110501` (#28).
- ☑ `20015` Real-Postgres (pglite) integration-test harness + CI app-boot smoke
  - **Delivered:** the test/CI safety net that `20004`–`20011` build on, forced by `20002` where the hand-rolled fake-DB harness masked two real bugs (a boot-time TS-execution crash and a `uuid`-column violation), both caught only by manual live-Postgres QA. `createTestDb()`/`seedBaseOrg()` (`apps/api/src/__tests__/helpers/test-db.ts`) — in-memory PGlite (`@electric-sql/pglite`) via `drizzle-orm/pglite`, applying the **real** `apps/api/drizzle/*.sql` migrations (schema parity: enums, `uuid`, `NOT NULL`, FKs, uniques); handle cast to `NodePgDatabase<typeof schema>` so no service/`buildApp` signatures changed; cross-platform migration path via `fileURLToPath`. `test-db.test.ts` proves teeth (non-uuid into a `uuid` column throws — the fake db accepted it). Ported `audit/service.test.ts` fully + the DB-writing cases of `routes/qbo-webhook.test.ts` (success/unknown-realm/multi-notification/login-regression) to real PG; pure signature/parse/503 cases stay on the fast fake db. **CI app-boot smoke** (`.github/workflows/ci.yml`, `verify` job) — `node -e "import('./src/app.ts')"` under real Node 24 (matches the Dockerfile), dummy env, so a strip-only-TS-incompatible construct fails the pipeline (Vitest's esbuild compiles it away). `@electric-sql/pglite` added as a **devDependency** (prod image unaffected). Two-tier test-story note in `docs/architecture-decisions.md`. **Verified:** 208 api + 50 web green, `docker build` clean (+ boot smoke passes in-image); QA + review both independently proved teeth (reintroduced a parameter-property → `ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX` exit 1; reverted the `20002` `localId` fix → the ported real-PG test fails 500 where the fake passed silently). No product/route/schema/migration change; `apps/web` untouched. **review-approved.** Non-blocking follow-ups: pglite may marshal `numeric` differently from node-postgres (money-column tests on `createTestDb()` should note it); migrate remaining fake-db cases later; boot smoke covers `app.ts`'s import graph (extend if a standalone entrypoint is added). Squash-merged as `4572aee` (#29).
- ☑ `20014` CD (GitHub Actions, on merge to `main`): OIDC role → build → ECR → gated migrations (`aws ecs run-task`) → register task-def revision → `aws ecs update-service`
  - **Delivered:** the continuous-deploy pipeline. `.github/workflows/deploy.yml` (push to `main` + `workflow_dispatch`): assumes an AWS role via **GitHub OIDC** (`id-token: write`, `role-to-assume: vars.CD_ROLE_ARN`, **no static keys**) → `amazon-ecr-login` → build+push image (`docker build --target runner`, tagged `github.sha`) → register a new ECS **task-def revision** (from `describe-task-definition` piped through the render helper) → **gated migration** via one-off `aws ecs run-task` against that NEW revision (`wait tasks-stopped` + assert container `exitCode 0`; any non-zero exit `exit 1`s the job **before** `update-service`) → `aws ecs update-service` + `wait services-stable`. `concurrency: {group: deploy-main, cancel-in-progress: false}` serializes overlapping merges. All AWS identifiers via `vars.*` (region, role ARN, cluster/service/family/container, subnets, SGs, ECR repo) — no hard-coded ARNs/account-ids; the workflow never runs `terraform` and never touches Route53 (Terraform owns infra + stands up the service with `lifecycle.ignore_changes=[task_definition,desired_count]`; CD owns only revisions + the service update, per `docs/design-decisions.md` §"Deploy and IaC boundary"). **Register-before-migrate is deliberate** — migrations must run against the new image or the release's own migration files are skipped; registration is metadata-only and moves no traffic. `Dockerfile` made multi-stage: new prod **`runner`** target (`pnpm install --prod --frozen-lockfile --filter @invoicing/api`, ships `apps/api/drizzle/` migrations, `CMD … start`); the **`dev` stage stays the default target** so docker-compose and CI's `docker build` are unchanged. `apps/api/src/deploy/render-task-def.ts` — pure `renderTaskDef({template,image,containerName})` (`structuredClone`, throws on missing container, strips ECS read-only fields `taskDefinitionArn/revision/status/requiresAttributes/compatibilities/registeredAt/registeredBy/deregisteredAt`) + a `pathToFileURL`-guarded CLI, invoked the same way as `db:migrate` (Node 24 native TS). **Verified:** 210 api (incl. 5 render-task-def cases) + 50 web tests pass, `biome ci` + `pnpm -r typecheck` clean, `actionlint` clean, both Docker targets build (dev default unbroken). QA confirmed the migration gate fails-closed (no false-green where a failed/absent migration lets `update-service` run — JMESPath `[0]` on a missing exitCode → `"None"` ≠ `"0"` → job fails), register-before-migrate coherence, and the no-secrets/no-terraform/no-Route53 boundary; **review-approved** (confidence: no scope creep; the render helper strips exactly what `register-task-definition` rejects; runner correctly excludes the drizzle-kit devDep since runtime `db:migrate` uses `drizzle-orm/node-postgres/migrator`). **Not runnable E2E against AWS yet** — target ECR/ECS/RDS/OIDC infra is Phase 3 (`30001`/`30009`/`30005`); GitHub `vars`/`secrets` populated there. Non-blocking follow-ups (file against `30001`): add `USER node` to the runner stage; inspect `run-task` `failures[]` for clearer placement-failure logs; enable ECS deployment circuit-breaker rollback. Squash-merged as `d49192f` (#30).
- ☑ `20003` Refetch: when a webhook payload is incomplete, fetch full invoice/payment state from QBO before applying
  - **Delivered:** the QBO **data-API read client** + `refetchEntity` primitive that `20004`/`20007` consume — a read-only library primitive (no route, no webhook wiring, no DB writes; applying is 20007). `apps/api/src/qbo/api-client.ts` — injectable `QboApiClient`; `getEntity()` does `GET /v3/company/{realmId}/{Type}/{id}?minorversion=73` (base URL per `config.qbo.environment` sandbox/production, `Authorization: Bearer` + `Accept: application/json`), unwraps the `{Invoice:{...}}` envelope. **Typed error taxonomy for 20011:** 401→`QboAuthError`, 404→`QboNotFoundError`, 429/5xx→`QboApiError{retryable:true}`, other non-2xx/malformed→`{retryable:false}`; bearer never logged/thrown. `apps/api/src/qbo/refetch.ts` — `refetchEntity()` composes `getValidAccessToken` (20001 refresh-on-demand) → `getEntity`; `mapNotificationToEntityType` (pure). New typed errors in `qbo/errors.ts` (plain-field constructors — no TS parameter properties). `qboApiClient` injection seam in `plugins/qbo.ts` + `app.ts` mirroring `qboOAuthClient`. **Verified:** 224 api (208→224) + 50 web green, `biome ci` + typecheck clean, `docker build` clean, CI **boot smoke** exit 0, web build 3 pages. `api-client.test.ts` (fake fetch — URL/headers per env, envelope unwrap, full error matrix, bearer-leak check), `refetch.test.ts` (`createTestDb()` real pglite — token-refresh path asserts the refreshed token reaches `getEntity`, no-connection, typed-error propagation, notification mapping). **review-approved.** Non-blocking follow-ups (downstream): retryable set omits 408/425/509; `QboEntityEnvelope` untyped (20004/20007 narrow); one bearer-leak sub-assertion sits inside a catch. Rebased past the parallel `20014` CD merge (disjoint). Squash-merged as `af69437` (#31).
- ☑ `20004` Mapping layer: entity-typed `SyncLink` resolution (`Contact` / `Account` / `Item` / `Transaction` ↔ QBO id + type), including chart-of-accounts / GL accounts
  - **Delivered:** the heart of the sync model — the DB-backed mapping service over the existing `sync_links` table + pure natural-key matchers that outbound (`20006`), inbound (`20007`), and idempotency (`20005`) build on. `apps/api/src/qbo/sync-link-service.ts` (org-scoped, `db`-first so it composes in a `tx`): `resolveQboType(entityType, txnType?)` (`contact→Customer`, `account→Account`, `item→Item`, `transaction+customer_invoice→Invoice`, `+payment→Payment`, else `UnmappableEntityError`); `findLinkByLocal`/`findLinkByQbo` (org-scoped); `upsertLink` — idempotent select-then-branch inside `db.transaction`, respects both uniques (`unique(orgId,entityType,localId)`, `unique(orgId,qboType,qboId)`), throws `ConflictingLinkError` on a local→different-qbo or qbo→different-local relink (never a silent overwrite); `setLinkState`/`markSynced`/`markConflict`/`markFailed`; `resolveTransactionDeps` — reference-data-first dependency **report** (contact + distinct line accounts + distinct line items → `allLinked`/`unlinked`, `DepStatus.link` carries state) that 20006 calls before pushing a document. `apps/api/src/qbo/natural-key.ts` — pure tri-state matchers (`match`/`none`/`ambiguous`, never auto-links ambiguous): contact by normalized email (case/space-insensitive) with displayName fallback only when no email; invoice by docNumber+total+date (or total+date+customer without a docNumber); money compared via `toCents` (integer cents, never float). `apps/api/src/qbo/errors.ts` — `UnmappableEntityError`, `ConflictingLinkError` (plain-field constructors, Node-24 boot-smoke safe). `docs/design-decisions.md` Mapping section extended. **No route, no schema, no migration** — mapping library only; QBO query-API candidate *fetching* deferred to inbound/reconciliation (`20007`). **Verified:** re-ran full pipeline after taking over an interrupted session — 264 api (incl. new sync-link + natural-key suites, 35 cases) + 50 web tests pass, biome + typecheck clean, `docker build` clean, Node-24 boot smoke `boot-ok`, web build still exactly 3 prerendered pages. QA proved the conflict/idempotency/org-isolation/money-as-cents assertions are non-tautological; **review-approved** (clean, correct, org-scoped, no scope creep). Non-blocking follow-ups: optionally map raw PG `23505` → `ConflictingLinkError` for the concurrent same-pair race (DB uniques already prevent any bad row; single-writer sync); `20006` must decide whether a non-`synced` reference link gates the document push. Squash-merged as `b6cbfad` (#32).
- ☑ `20005` Idempotency: event dedup by external event id, idempotency keys on writes, `ON CONFLICT` upserts — no duplicate records or repeated writes
  - **Delivered:** the idempotency layer so duplicate/redelivered QBO webhooks never create duplicate records or repeated writes. New `processed_events` table (`apps/api/src/db/schema.ts` + drizzle-generated `0005_wakeful_silver_samurai.sql`; `unique(org_id, event_key)`, FK → `organizations`, `(org_id)` index). `apps/api/src/qbo/event-dedup.ts`: `buildEventKey({realmId,name,id,operation,lastUpdated})` (pure tuple `realmId:name:id:operation:lastUpdated`, 4-tuple fallback when `lastUpdated` absent) + `recordEventIfNew(db, …)` — **atomic** `INSERT … ON CONFLICT (org_id, event_key) DO NOTHING RETURNING id` (no separate SELECT, so concurrent double-fires can't both win); returns `true` on first delivery, `false` on redelivery. Wired into `apps/api/src/routes/qbo-webhook.ts` per entity **before** the audit-write: first delivery writes the existing `qbo.webhook.received` row; a duplicate writes one `qbo.webhook.duplicate`/`outcome:'skipped'` row and is skipped (still acks `200`; unknown-realm ack-and-skip unchanged). Pure `outboundIdempotencyKey({orgId,entityType,localId,localVersion})` = `orgId:entityType:localId:v<localVersion>` (`apps/api/src/qbo/idempotency-key.ts`) for outbound sync (`20006`). `docs/design-decisions.md` §Idempotency updated. **Verified:** 280 api (incl. new dedup, key, and end-to-end webhook-dedup suites with anti-tautology row-count assertions — same body twice → exactly one `received` row; genuine re-edit with a new `lastUpdated` NOT deduped; per-entity dedup in a multi-entity notification) + 50 web tests pass, biome + typecheck clean, `docker build` clean, Node-24 boot smoke `boot-ok`, migration applies via `createTestDb()`. **Review-approved.** Non-blocking follow-ups carried into 20006/20007/20011 briefs: (1) the dedup insert + apply are separate statements — 20007/20011 must claim + apply in ONE `db.transaction` (`recordEventIfNew` accepts a `tx`) so a mid-apply crash lets Intuit's redelivery re-drive rather than being deduped away; (2) `buildEventKey` uses unescaped `:` (safe for QBO numeric ids + terminal `lastUpdated`; escape if reused with free-text); (3) `lastUpdated`-absent fallback can falsely dedup two distinct edits — don't treat inbound dedup as a hard guarantee for lastUpdated-less payloads. Squash-merged as `099fbe8` (#33).
- ☑ `20006` Outbound sync: propagate internal create/edit/void of invoices and payments to QBO
  - **Delivered:** the outbound half of the sync engine. `apps/api/src/qbo/outbound-sync.ts` — invoked **best-effort after the local `db.transaction` commits** (never inside it): reference-data-first + **`synced`-gated** (`ensureEntitySynced` pushes contact + each distinct line account/item only when their `SyncLink` isn't already `synced`; a `pending`/`failed` ref is treated as unlinked and re-pushed before the document — resolves the 20004 review note); **outbound safety** via create-vs-update by existing `SyncLink` (a retried push issues a sparse UPDATE with Id + stored SyncToken — refetched via `getEntity` when the cached token is missing — never a duplicate CREATE); **void = QBO `?operation=void`** (never delete; a never-synced void is a safe skip); **failure isolation** — any QBO/mapping error → link `failed` + `outbound/failure` audit + `{status:'failed'}`, the local write + HTTP response never affected; **no connection → total no-op** (link stays `pending`). `markSynced`/`upsertLink` persist `qboId` + `qboSyncToken` + `localVersion` + `lastSyncedAt`. Extended `apps/api/src/qbo/api-client.ts` with `createEntity`/`updateEntity`/`voidEntity` (reusing the 401→auth / 404→notfound / 429/5xx→retryable mapping). `routes/invoices.ts` + `routes/payments.ts` wired best-effort; `docs/design-decisions.md` §Mapping/§Idempotency/§Delete-vs-void updated. Tests use an injectable fake QBO write client + `createTestDb()`. **Verified:** 311 api (incl. new outbound-sync, payload-builder, client-write, and route-outbound suites — idempotency create-count stays 1 on re-push, failure-isolation with surviving local row, ref-gating, void/never-synced-noop, no-connection-noop, payment LinkedTxn, ConflictingLinkError→failed) + 50 web tests pass, biome + typecheck clean, docker build clean, Node-24 boot smoke `boot-ok`. QA run by planner (QA subagent hit a transient session limit); **review-approved** — all three hard guarantees hold (network outside the txn, no throw escapes to the handler, no duplicate QBO record). Non-blocking handoffs to later tasks: (1) **20011** — a first-ever push failure leaves the link `pending` not `failed` (domain create seeds no link row; `markFailed` is UPDATE-only), so retry must scan `pending`/no-synced-link or seed a link at create; (2) **20008/20011** — create-then-persist duplicate window (mitigate with a claimed/in-flight link before push and/or 20007 natural-key reconciliation before CREATE); (3) routes await the push before responding — decouple via queue/fire-and-forget in the retry task; (4) `buildQboPayment` sends `TotalAmt = txn.total` — note for the live-sandbox pass. Squash-merged as `f4ed324` (#34).
- ☑ `20007` Inbound sync: apply QBO-originated changes to internal records
  - **Delivered:** the inbound half of the sync engine + the correctness fix carried from 20005's review. `routes/qbo-webhook.ts` restructured so each entity is processed as **refetch full QBO state (`refetchEntity`, OUTSIDE any tx) → ONE `db.transaction` claiming (`recordEventIfNew(tx,…)`) and applying (`applyInboundEntity(tx,…)`) atomically** — a crash after refetch but before commit rolls back the dedup claim so Intuit's redelivery re-drives the event (no dropped events, no network inside a tx); a failed refetch writes a `failure` audit and leaves the event **unclaimed** (retryable). `apps/api/src/qbo/inbound-sync.ts` — `applyInboundEntity` + pure mappers (`qboInvoiceToLocalPatch`/`qboPaymentToLocalPatch`, which **structurally exclude amount/line/status fields** so an Update can never unbalance the ledger) + org-scoped candidate loaders. **Apply matrix:** linked Update → patch QBO-owned metadata (DocNumber/TxnDate/DueDate/memo); linked Void/Delete → local void (zero ledger, delete `payment_applications`, recompute invoice status/balance — logic verified byte-for-byte equivalent to canonical `voidInvoice`/`voidPayment`, and more robust: recomputes every affected invoice); unlinked → natural-key link (single confident match only, via the 20004 matchers with a role-reversal alias) or `skipped` audit (never auto-create, never guess); inbound CREATE of brand-new local rows **deferred** (documented, recorded as `no_match:create_deferred` for manual linking); Merge/Emailed/unsupported-type → skipped. `markSynced` stamps SyncToken + localVersion (the compare seam for 20008/20010). `docs/design-decisions.md` §Idempotency/§Mapping/§Failure-handling updated. Tests use `createTestDb()` + injectable fake apiClient (canned refetch payloads). **Verified:** 345 api (incl. 32 new inbound-sync + 14 webhook tests — atomicity proven by a mid-apply throw leaving 0 `processed_events` rows via the shared tx, idempotent redelivery proven by unchanged persisted state, full apply matrix with real row/value assertions, ledger balanced/zeroed on void) + 50 web tests pass, biome + typecheck clean, docker build clean, Node-24 boot smoke `boot-ok`. **Review-approved** (the hand-rolled inbound void/recompute is faithful to canonical service logic; no scope creep). Non-blocking carry-forwards: (20010) inbound Void of a locally *paid* invoice leaves the applied payment intact → contact A/R net-negative (the voided-in-both conflict case — 20010 decides whether to reverse/flag the payment); (20008) the link's `localVersion` snapshot lags the txn row by one after a link+patch — normalize in the stale-skip comparison; (boundary) metadata TxnDate patch doesn't re-post the ledger `entryDate` (intended per the metadata-only scope). Squash-merged as `d77db20` (#35).
- ☑ `20008` Ordering: handle out-of-order events (version/updated-at guards, skip stale writes)
  - **Delivered:** the ordering guard so out-of-order / replayed events don't clobber newer state. `apps/api/src/qbo/ordering.ts` — pure `isStaleInboundApply({storedSyncToken,storedLastSyncedAt},{incomingSyncToken,incomingLastUpdated})` + `parseSyncToken`: primary compare = QBO `SyncToken` as int (apply iff incoming > stored; **equal → stale/skip**, idempotent); fallback = `MetaData.LastUpdatedTime` vs stored `lastSyncedAt` when a token is missing/unparseable; first-sync (no stored token AND no lastSyncedAt) → not stale (never skip a first apply); empty/garbage token → treated as missing (not 0), never throws. Wired into `inbound-sync.ts` linked Update/Void branches (invoice + payment) **before any mutation** — a stale event returns `{action:'skipped', reason:'stale_ignored'}` + audit, without patching or downgrading the stored token; a stale Void is skipped (never resurrects/zeroes newer state). **Fixed the 20007 seam:** `linkUnmatchedInvoice` re-stamps `sync_links.localVersion` to the post-patch `transactions.version`. `outbound-sync.ts` — symmetric redundant-write guard: when a link's `localVersion >= transactions.version`, skips the sparse UPDATE (`outbound/skipped`, `already_current`) instead of a redundant push; never skips a genuine new edit; create/void paths unaffected. `docs/design-decisions.md` §Ordering updated. **Verified:** 364 api (incl. new ordering unit tests + inbound stale-skip/seam + outbound already_current suites — stale-skip asserts the PERSISTED record is unchanged and the link token not downgraded, equal-token boundary, genuine-newer resumes, payment stale-skip) + 50 web tests pass, biome + typecheck clean, docker build clean, Node-24 boot smoke `boot-ok`. **Review-approved** — comparator exact, guards before any mutation, seam fix correct, no conflict/retry logic leaked. **One-sided staleness only** — both-sides-changed conflict is 20010. Follow-ups: (20010) layer both-sided detection at the same seam via a local-dirty check (`transactions.version > sync_links.localVersion`); the stale-Void-after-Update case is the likely spot 20010 reclassifies as a conflict; (20011) a push that lands at QBO but whose link-write throws leaves a harmless redundant re-push — retry/reconcile should distinguish "push landed" from "link recorded". Squash-merged as `776c17d` (#36).
- ☑ `20009` Delete-vs-void: treat delete and void as distinct actions matching QBO semantics
  - **Delivered:** the delete-vs-void split for invoices + payments, both directions. New nullable `transactions.deletedAt` (migration `0006_noisy_silhouette.sql`) is the **soft-delete** marker — orthogonal to `status`, never a hard row delete: a delete zeroes the ledger and stamps `deletedAt` but **retains the row + `sync_links`** so a re-sync never re-creates it. All read paths exclude it (`isNull(deletedAt)` on `getInvoice`/`listInvoices`/`loadInvoiceForUpdate` + payment loaders); the web Dashboard aggregates client-side over the already-filtered list (no leaking server aggregate). **Local delete:** `deleteInvoice`/`deletePayment` behind `DELETE /api/invoices/:id` & `/api/payments/:id`; refuses `partially_paid`/`paid` invoices → `InvalidStateError` → **409** (codebase-consistent with the void/pay guards via `mapServiceError`); idempotent no-op `{action:'skipped', reason:'already_deleted'}` on repeat. **Outbound:** new QBO client `deleteEntity` (`?operation=delete` with `{Id,SyncToken}`) + `deleteDocument` (mirrors `voidDocument`, audit `outbound_delete`); the outbound entry checks `deletedAt` **before** `status` (voided-then-deleted pushes a delete); never-synced delete → no QBO call. Void path byte-for-byte unchanged (`VOID_OPERATIONS` narrowed to `{Void}`, `DELETE_OPERATIONS={Delete}`, `VOID_OR_DELETE_OPERATIONS` retained where the two stay equivalent). **Inbound:** QBO `Delete` → `softDeleteLocalInvoiceRow`/payment soft-delete + audit `qbo.inbound.delete`; QBO `Void` → local void (unchanged); the 20008 `isLinkStale` ordering guard runs on the delete branch **before any mutation** (stale delete → skipped, never soft-deletes over newer state); once `deletedAt` is set any further inbound op is a terminal no-op skip (link retained, never re-created). Inbound delete of a locally-**paid** invoice → soft-delete anyway to mirror reality, `payment_applications` left intact + audited (the delete-of-paid-in-both conflict is a **clean 20010 seam** — no payment reversal attempted here). `docs/design-decisions.md` §Delete-vs-void rewritten. Also rewrote 4 hand-rolled `fakeDb` test harnesses to an order-independent drizzle-AST token scanner (the plain positional `columns[]/params[]` zip desynced on the new `isNull` node) — QA mutation-tested the new tests to prove non-vacuousness. **Verified:** 383 api (incl. the headline anti-tautology Delete-vs-Void *distinct-persisted-state* test, no-re-creation-after-delete, stale-inbound-Delete skip for invoice + payment, outbound delete=1/void=0 call-count assertions, 409 paid-delete guard, idempotent re-delete) + 50 web tests pass, biome + typecheck clean, docker build clean, Node-24 boot smoke `boot-ok`, migration `0006` applies via `createTestDb()`. **QA passed** (twice — full run + a lean re-confirm with a guard-mutation proof) and **review-approved** (two-way distinction correct, no read path leaks soft-deleted rows, void path preserved, guardrails/idempotency/ordering hold, 20010/20011/20012 seams left clean, conventions met). Non-blocking carry-forward → **20010:** local `deletePayment` recomputes via `loadInvoice` (which filters `isNull(deletedAt)`), so deleting a payment whose invoice was already inbound-soft-deleted (paid-in-both) throws `NotFoundError`/404 — exactly the conflict edge 20010 owns. Squash-merged as `f916659` (#37).
- ☑ `20010` Conflict detection + policy: flag invoices edited in both systems since last sync as `conflict`, no silent overwrite; resolution UI to pick the winning version
  - **Delivered:** the conflict policy — when a record changed in **both** systems since the last successful sync, the engine flags it `conflict`, stops writing it in **either** direction, and requires a user to pick the winner (last-write-wins explicitly rejected for financial records). **Detection:** pure `apps/api/src/qbo/conflict.ts` `isBothSidesConflict` (local-dirty — `transactions.version > sync_links.localVersion`, null localVersion = not-dirty — AND the incoming QBO change genuinely newer, i.e. `isStaleInboundApply` false). Wired into `inbound-sync.ts` **after** each linked Invoice/Payment Update/Void/Delete branch's stale-gate and **before** any mutation (`handleConflictIfAny`): `markConflict` + stamp new `sync_links.conflict_detected_at` (migration `0007_tired_trauma.sql`) + audit `qbo.inbound.conflict`; nothing applied, stored SyncToken/localVersion left as the pre-conflict snapshot. Repeated inbound while conflicted is held idempotently (`handleAlreadyConflictHold`, `conflict_held`). **Both-direction stop:** `outbound-sync.ts` `conflict_blocked` entry guard — no create/update/void/delete pushed to QBO while a link is `conflict`. **Resolution:** `GET /api/conflicts` (org-scoped, joined to the local transaction, soft-deleted included) + `POST /api/conflicts/:linkId/resolve {winner:'local'|'qbo'}` — `winner:'local'` force-pushes local→QBO (a `force` path bypassing the conflict + already-current guards, reachable ONLY from resolution); `winner:'qbo'` refetches and applies QBO's **terminal** state locally (`bypassConflict`), converging via `Delete > Void > Update` precedence over the current episode's audit rows (you can't update a voided/deleted QBO entity), windowed by `conflict_detected_at`. Either way the link returns to `synced` + `conflict_detected_at` cleared; audits `conflict.resolved`. 409 (non-conflict) / 404 (unknown/cross-org) / 400 (bad winner); a force-push failure leaves the link `conflict` + `conflict.resolve_failed` (no partial commit). **Web:** `/conflicts` page (behind auth) with **Keep mine** / **Use QuickBooks version** actions, Sidebar entry + count badge, `SyncStatusBadge` conflict state. Reclassifies the previously-silent voided-in-both (A/R-negative, 20007) and deleted-in-both (404, 20009) seams as visible conflicts. **A latent bug found + fixed during review-loop:** `markConflict` stamped `conflict_detected_at` with JS `new Date()` (mid-tx wall-clock) while the audit `created_at` uses Postgres `now()` (pinned to tx START, earlier) — so the `>= conflict_detected_at` recovery window silently excluded the conflict-raising row, defaulting every `winner:'qbo'` to the `Update` fallback; fixed by stamping with SQL `now()` too (both = tx-start). **Verified:** 417 api (detector unit tests + local-clean anti-tautology control; conflict on Update/Void/Delete for invoices + payments; voided-in-both-paid stops with `payment_applications` intact + balance ≥ 0; outbound `conflict_blocked` zero-QBO-calls; resolution round-trips + 409/404/400 + `resolve_failed`; three **real-audit-trail** regression tests for the terminal-precedence recovery — Void-then-held-Update → local void, Delete-then-held-Update → soft-deleted, Update-only control) + 59 web tests pass, biome + typecheck clean, docker build clean, Node-24 boot smoke `boot-ok`, migration `0007` applies via `createTestDb()`. **QA rejected attempt 1** (winner:'qbo' replayed the most-recent audit op instead of QBO's terminal state — Void-then-held-Update left an invoice `open`), fixed in attempt 2 (terminal precedence + the timestamp fix + real regression tests), **QA re-passed** (mutation-tested both fixes independently) and **review-approved**. Non-blocking follow-ups (live-QBO-only, user-gated at phase-end): winner:'local'/'qbo' where QBO already hard-deleted → refetch/push 404; `ensureEntitySynced` upsertLink doesn't pass `conflictDetectedAt:null` (harmless — reference-data links never conflict). Out of scope, seams left clean: retry/backoff (20011), Integrations sync-log/health/manual-retry UI (20012), full payment reversal when QBO wins over a paid invoice (Phase 4). Squash-merged as `792e8b5` (#38).
- ☑ `20011` Failure handling: retry with backoff, safe recovery from partial success (timeout after a write), failed-item state for manual retry
  - **Delivered:** the outbound failure-recovery layer. **`sync_links` becomes the retry-queue record** (migration `0008_certain_hellfire_club.sql`): `qbo_id` relaxed to **nullable** + new `retry_count`/`next_retry_at`/`last_error`. A link now models "a local entity we intend to sync," not only an already-synced pair. **First-ever failure is now retryable:** `markFailed` UPSERTs a `failed` link (seeding one with `qboId=null` when domain-create seeded none) — previously a first-ever outbound push failure left NO link at all (only an audit row) and was invisible to any retry loop (the 20006-review gap); never demotes a `conflict` link; `markSynced` + the outbound success paths clear the retry fields. **Backoff + sweep:** pure `computeBackoff` in `qbo/retry.ts` (30s base, ×2, cap 1h, terminal `nextRetryAt=null` at `MAX_RETRY_ATTEMPTS=8`); `runOutboundRetrySweep` (`qbo/retry-sweep.ts`) selects due `failed` links cross-org (skips orgs with no connection), re-drives create/update/void/delete **reusing the existing `sync*Outbound` machinery**; a guarded, non-overlapping, never-throw `setInterval` runs it in **`index.ts` only** (env `SYNC_RETRY_ENABLED`/`SYNC_RETRY_INTERVAL_MS`) — nothing in `app.ts`, so the test suite spawns no timers (asserted by `app.test.ts`). **Partial-success safety (timeout-after-write):** before a CREATE retry, `reconcileDocumentCreate`/`reconcileContactCreate` query QBO by natural key — a landed-but-unlinked create is **linked (no second create)**; an ambiguous match is left `failed` for manual resolution, never auto-linked (added optional `queryEntities` to the QBO client). **Failed-item API (the surface 20012's UI consumes):** `GET /api/sync/failures` (org-scoped, joined to the local transaction) + `POST /api/sync/failures/:linkId/retry` (force an immediate attempt; 409 non-failed / 404 unknown/cross-org; audit `sync.manual_retry`). **A latent bug fixed during dev:** `upsertLink`'s conflict guard threw `ConflictingLinkError` on the first `qboId` assignment to a previously-`failed` null-qboId link (would break every CREATE retry) → now only enforces the guard when `byLocal.qboId !== null`, keeping the `byQbo`-claimed-elsewhere check. **A perpetual-retry-loop bug fixed during review-loop:** a never-synced (`qboId=null`) `failed` link whose local txn was later soft-deleted or locally voided looped forever (the void/delete outbound skip never cleared it, so it was re-swept every tick, never terminal, un-clearable) → new `deleteNeverSyncedFailedLink` (scoped `state='failed' AND qboId IS NULL`) clears it at both the sweep and request-time skip paths, with a new `'cleared'` outcome. **Verified:** 458 api (backoff unit tests; first-ever-failure seeding + success-clears control; sweep retry/backoff/terminal; reconcile exactly-one-create + ambiguous-not-linked; conflict immunity; endpoint negatives + audit; timer-isolation; terminal-local clear for sweep + request-time × soft-delete + void, with an open-txn anti-regression control) + 59 web pass, biome + typecheck clean, docker build clean, Node-24 boot smoke `app.ts` + `index.ts` clean, migration `0008` applies via `createTestDb()`. **QA passed** (rigorously — reproduced the pglite cold-start timeout noise as pre-existing on the merge-base, and mutation-tested the reconcile + guard + the terminal-local-clear scoping). **Review rejected attempt 1** (the perpetual-retry-loop defect), fixed in attempt 2, **QA re-passed** and **review-approved** (defect closed, delete doubly-gated, no over-deletion). Non-blocking follow-ups: account/item create-retry has no natural-key reconcile → a landed-but-unlinked reference create could duplicate on retry (scoped out — reference-data-first, 20004); manual retry of an already-terminal link stays terminal (cosmetic note for 20012's UI wording). Out of scope, seams left clean: Integrations page/health/sync-activity-log/manual-retry **button** (20012), sync-engine E2E suite (20013), inbound persisted-retry (inbound relies on Intuit webhook redelivery, 20007), request-path push decoupling, payment reversal (Phase 4). Squash-merged as `540692d` (#40).
- ☑ `20012` Integrations page: connect/disconnect QBO, connection health, chronological sync activity log, manual retry of a failed item
  - **Delivered:** the operator UI over the merged sync engine — no new sync behavior, one new read endpoint. **`GET /api/sync/activity`** (`apps/api/src/audit/activity.ts` `listSyncActivity(db,orgId,limit)` + `apps/api/src/routes/sync-activity.ts`, registered in `app.ts`): org-scoped, newest-first (`desc(createdAt), desc(id)`), `?limit` Fastify-schema-bounded 1–200 **and** code-clamped, default 50, over `sync_audit_logs`; 401 unauth; no migration. Reuses as-is the existing connect/disconnect/status (`routes/integrations.ts`), failed-item list + manual retry (`routes/sync-failures.ts`), and conflicts (`routes/conflicts.ts`). **Web:** rewrote the `10012` placeholder `apps/web/src/routes/Integrations.tsx` into the real page on the Clearbook primitives — connection card (realm + token-expiry health from `connectionStatus`, **admin-gated** Connect/Disconnect via `useOutletContext` role, one-time `?connected=1`/`?error=` banner then param-cleared), a conflicts callout linking to `/conflicts` (no duplicate resolution UI), a "Needs attention" failed-items list with per-row **Retry** (inline 409/503 handling), and the sync activity log (mixed-outcome styling). Added `qboStatus`/`connectQbo`/`disconnectQbo`/`listSyncFailures`/`retrySyncFailure`/`listSyncActivity` + types to `apps/web/src/lib/api.ts`. **Attempt-2 fix (QA reject):** `GET /api/integrations/qbo/status` was still `requireRole('admin')` from 20001, so members got a 403 and the page errored — relaxed to `app.authenticate` (members read connection health; `connectionStatus()` serializes **no tokens**, only `{connected, realmId, accessTokenExpiresAt, refreshTokenExpiresAt}`); `connect`/`callback`/`disconnect` stay admin-gated. Tests: `sync-activity.test.ts` (6 — org-scoping/ordering/limit-cap-and-default/empty/401 on real pglite, mutation-proven) + `Integrations.test.tsx` (12) + a member-status regression in `integrations.test.ts` (member `/status` 200 no-token-leak; connect/disconnect still 403, mutation-proven). **Verified:** typecheck 0, 465 api (stable serialized) + 71 web, biome/docker/Node-24-boot-smoke/web-build (exactly 3 prerendered pages) all clean. **QA rejected attempt 1** (member 403 on `/status`), fixed attempt 2, **QA re-passed** (mutation-tested the fix + live-verified member read-only access — not-connected AND connected/realm variants, no tokens in DOM, zero console errors — against a fresh docker-compose stack via Playwright, plus all admin flows: connect 503-not-configured, connected→realm/health→disconnect, failed-item retry 409/503, conflicts callout, both URL banners, activity styling), and **review-approved** (auth-gate relaxation confirmed token-safe; new endpoint org-scoped + limit-bounded + non-tautological tests; reused endpoints untouched; no schema/migration; no TS parameter-properties; commit hygiene clean). Commits `1bc10f3` (impl) + `e4837d5` (member-status fix). Squash-merged as `e9e0c55` (#41).
- ☑ `20013` Sync engine tests: duplicate webhook, out-of-order, edited-in-both, delete-vs-void, partially-paid edit, timeout-after-write, retry-after-partial-success, pre-existing unlinked invoices
  - **Delivered:** the end-to-end edge-case suite over the merged sync engine (20005–20011) — **test-only, no production/schema change**. `apps/api/src/__tests__/sync-engine.e2e.test.ts` (11 `it()`s) drives each of the 8 named PRD edge cases + 1 interplay case through the **real** entry points (`app.inject` against the signed `/api/integrations/qbo/webhook` route, the invoice/payment HTTP routes, `runOutboundRetrySweep`) against real Postgres (pglite via `createTestDb()`) with injectable fake QBO clients (`fakeApiClient` canned refetch + `createFakeQboWriteClient({failOn})`), asserting persisted table state + `sync_audit_logs` rows + `client.countOf(...)` — non-tautological throughout. Cases: (1) duplicate webhook → byte-identical no-op, one `processed_events` row + one `qbo.webhook.duplicate` audit; (2) out-of-order → stale SyncToken skipped without downgrading the stored token, genuinely-newer applies; (3) edited-in-both → `conflict` raised, neither side applied, next outbound push blocked with zero QBO calls; (4) delete-vs-void → inbound Void zeroes ledger + flips `status`, inbound Delete soft-deletes without touching `status`, no resurrection on a later inbound; (5) partially-paid → local edit 409s, and a genuinely-newer inbound update after a payment is **`conflict`-blocked** (documented 20010 behavior: `recordPayment`'s `recomputeInvoice` bumps `transactions.version` without resyncing the invoice's own `sync_links.localVersion`, so the invoice is "locally dirty" — the plan's assumption of a clean apply was corrected to match real, reviewed behavior; flagged to planner, no production change); (6) timeout-after-write → a `failed`/`qboId=null` link coexisting with an already-landed QBO entity; (7) retry-after-partial-success → the sweep reconciles via a natural-key query with **no second create**, plus a plain transient-failure-then-recovery case with exactly one successful create; (8) pre-existing unlinked invoices → a confident natural-key match links without duplicating, an ambiguous (2-candidate) match skips with no mutation; + interplay: identical redelivery of a stale event. **Verified:** `pnpm -r typecheck` 0, `pnpm -r test` **476 api** (465→476, +11) + 71 web (stable serialized; default-parallel hits the known pre-existing pglite flake in unrelated files only), `biome ci .`/`docker build`/Node-24 boot-smoke/web-build (exactly 3 prerendered pages) clean; the new file alone 11/11. **QA passed** attempt 1 with **two mutation spot-checks** proving the suite bites the real engine (neutering `ordering.ts isStaleInboundApply` failed exactly the out-of-order + interplay tests; neutering `event-dedup.ts recordEventIfNew` failed exactly the duplicate + interplay tests; the other 9 stayed green) and source-tracing that the 5(b) conflict pivot is genuine documented behavior. **Review rejected attempt 1** (scenario 5(b)'s `it()` title contradicted its own correct assertions), fixed by a one-line title rename (attempt 2, `e0bd6f1`), **review-approved**. Commits `3cc4c67` (suite) + `e0bd6f1` (title fix). Squash-merged as `0e25e7d` (#42). Closes the Phase-2 sync-engine test coverage; deferred Phase-1 UI completions 10018/10019 remain.
- ☑ `20016` Fix: all outbound QBO writes (Customer/Invoice/Account/…) rejected by the live sandbox with a generic "Unsupported Operation" fault
  - **Delivered:** found via first live-sandbox connect (user-gated, post-20013) — every outbound `POST` (create/update/void/delete) 400'd against a real Intuit sandbox with `{"Fault":{"Error":[{"Message":"Unsupported Operation","Detail":"...is not supported."}]}}`, while reads (`GET`/query) on the same realm+token succeeded. Root-caused by isolating the failure outside the app entirely (bare `curl`, minimal bodies) across 3 sandbox companies and 2 developer apps before Intuit support identified it: **QBO's v3 REST routes require the entity-name URL segment lowercase** (`.../customer`, not `.../Customer`) even though the JSON body, query text, and response envelope all stay PascalCase (`{Customer:{...}}`). `apps/api/src/qbo/api-client.ts`'s `entityUrl` was interpolating the PascalCase `QboEntityType` straight into the path — worked in every unit test (fake `fetch`, case-insensitive) but never against the real API, since nothing before this exercised a live QBO connection. Fix: `entityUrl` now lowercases only the URL path segment (`entityType.toLowerCase()`); every other use of `entityType` (body keys, `queryEntities`' SQL-like query text, `unwrapEntity`) is untouched. Updated the 4 path assertions in `api-client.test.ts` accordingly. **Verified:** `api-client.test.ts` 15/15; live-reproduced against a real sandbox connection before AND after (create/update now 200, e.g. `Customer` create returning a real `Id`); user confirmed the previously-`failed` `sync_links` rows (contact + cascaded invoice) cleared via manual retry post-fix. Not caught by any existing test because the full suite runs entirely against injected fake QBO clients (by design, per `docs/architecture-decisions.md`'s no-live-Intuit-in-CI seam) — this class of bug is only visible against the real API, which is why it surfaced during the user-gated live-sandbox pass rather than in CI.

## Phase 3 — Infrastructure as code + deploy (`3000x`)

Goal: reproducible **GCP** deployment via Terraform, wired to the CD pipeline, held
under ~$30/mo.

**AWS → GCP pivot (Jul 8).** The AWS infra + CD landed first (`30001`/`30011` + CD
wiring, PR #44), but the AWS account was then blocked for compute, so the deploy
target moved to **Google Cloud**: Cloud Run (API) + Cloud SQL (Postgres) + Artifact
Registry + Secret Manager + Cloud Scheduler (retry sweep) + Firebase Hosting (web),
with CI authenticating via Workload Identity Federation. The reasoning is written up
in `docs/architecture-decisions.md` and `docs/design-decisions.md`. The AWS-specific
tasks below are superseded — kept for the record (their IDs are preserved), not as
forward work. Their combined GCP replacement is `30012`.

**Superseded — AWS (delivered in PR #44, then replaced by the GCP stack in `30012`):**

- ⊘ `30001` Terraform: RDS Postgres, ECR, ECS cluster + Fargate service, VPC/networking, IAM task roles → Cloud SQL + Artifact Registry + Cloud Run.
- ⊘ `30002` Terraform: Route53 + EventBridge → Lambda DNS re-point on task-IP change → **dropped entirely**; Cloud Run has a stable HTTPS URL.
- ⊘ `30003` Terraform: S3 + CloudFront frontend, `/api/*` → Fargate → Firebase Hosting with an `/api/**` rewrite → Cloud Run.
- ⊘ `30005` Wire CD to the Terraform-managed ECR/cluster/service → CD now builds to Artifact Registry, migrates via a Cloud Run Job, and rolls the Cloud Run service.
- ⊘ `30009` / `30011` Bootstrap Terraform: GitHub **OIDC** provider + CD role → **Workload Identity Federation** pool/provider + deployer service account (`infra/bootstrap`).

**Done (GCP):**

- ☑ `30012` Migrate the deploy target from AWS to Google Cloud (Cloud Run + Cloud Scheduler)
  - **Delivered:** the full GCP deploy stack, replacing the AWS one, held to ~$9–13/mo (Cloud SQL is essentially the whole bill). `infra/terraform`: Cloud SQL for PostgreSQL 17 (`db-f1-micro`, **Enterprise** edition — the cheapest that accepts the shared-core tier), Artifact Registry, a Cloud Run service (`cpu_idle=true`, scale-to-zero) + migration Cloud Run Job, Cloud Scheduler (drives the retry sweep), Secret Manager (DB URL / session secret / sweep token, generated by `random_password`), a Firebase Hosting site, and IAM. `infra/bootstrap`: Workload Identity Federation pool/provider + a deployer service account (replaces the AWS GitHub-OIDC role). `.github/workflows/deploy.yml` rewritten (WIF auth → build/push to Artifact Registry → migrate via the Cloud Run Job as a `--wait` gate → roll the Cloud Run service → `firebase deploy` the web bundle). `firebase.json` keeps the app same-origin via an `/api/**` rewrite → Cloud Run. **One app change:** an authenticated `POST /internal/retry-sweep` so the outbound sweep runs via Cloud Scheduler while Cloud Run scales to zero (the in-process `setInterval` is kept for local/compose, off in prod via `SYNC_RETRY_ENABLED=false`). Docs (README, CLAUDE.md, architecture/design decisions, both infra READMEs) rewritten for GCP, including the Cloud Run "why not serverless → how the sweep survives scale-to-zero" rationale. **Verified:** `terraform validate` (both stacks), typecheck, boot-smoke, `docker build`, and 485/485 api tests green; then applied to a real project (`clearbooks-501812`) and the CD pipeline ran green end-to-end — API healthy on Cloud Run, web live on Firebase Hosting, migrations applied against Cloud SQL, `/api/**` proxy confirmed same-origin. Squash-merged as PR #47 (+ follow-up fixes on `main`: `edition=ENTERPRISE`, `cpu_idle=true`, the secret-version dependency / `deletion_protection=false`, `user_project_override` for Firebase, and `SESSION_SECRET` on the migrate job). Bring-up runbook: `README.md` ## First-time deploy bootstrap.
- ☑ `30013` Manual production-seed workflow (`.github/workflows/seed.yml`, `workflow_dispatch` only)
  - **Delivered:** the deploy pipeline runs migrations but not seed, so prod started with no users. `seed.yml` runs the same idempotent `db:seed` as local (the **Acme Invoicing** org + `admin@`/`member@invoicing.test`, password `password123`, + the minimal chart of accounts) as a one-off Cloud Run Job with the same runtime SA, Cloud SQL connector, and required secrets (`DATABASE_URL` + `SESSION_SECRET`) as the migration job — created-or-updated in the workflow (it's a purely operational task) and reusing the existing GitHub Actions vars (no new ones). **Verified:** ran green against the live database; `admin@invoicing.test` / `password123` authenticates on the deployed site. Squash-merged as PR #48.
- ☑ `30014` Rename the session cookie to `__session` for Firebase Hosting
  - **Delivered:** on the deployed site, login returned 200 but `/api/auth/me` then 401'd, bouncing users back to login. Root cause: Firebase Hosting (which fronts the API via its `/api/**` rewrite) strips every request cookie **except** one literally named `__session`, so the `sid` cookie was set at login but dropped on every subsequent request. Renamed `SESSION_COOKIE` `sid` → `__session` (`apps/api/src/plugins/auth.ts`; `routes/auth.ts` already used the constant), updated the test helpers that hardcoded the name (extraction + injection), and documented the constraint in `architecture-decisions.md`. **Verified:** typecheck + Biome clean, 485/485 api tests pass; after deploy, login→`/me` returns 200 through the live Firebase→Cloud Run path and the session persists. Squash-merged as PR #49.
- ☑ `30016` Import invoices created in QuickBooks (inbound create + auto-link by QBO id)
  - **Delivered:** an inbound webhook for a QBO invoice with no local link and no natural-key match now **imports** it instead of skipping (`no_match`/`create_deferred`). `apps/api/src/qbo/inbound-sync.ts` `linkUnmatchedInvoice` branches on the match result: `ambiguous` still skips (never guessed), `match` links (unchanged), `none` → new `createLocalInvoiceFromQbo`. Pieces: `mapQboInvoiceLines` (pure, exported — maps `SalesItemLineDetail` lines → `InvoiceLineInput[]`, `Qty` default 1, `UnitPrice` fallback to `Amount`, skips every other `DetailType` so the mapped lines always sum to the sales total and the posted ledger is balanced by construction); `resolveOrCreateContactFromRef` (resolves the contact by `CustomerRef.value` via `sync_links`, else creates + links a new contact from `CustomerRef.name`, keyed to the QBO Customer id); `createLocalInvoiceFromQbo` (creates the invoice via the new shared `insertCustomerInvoice`, links it to the QBO id `synced`, audits `qbo.inbound.create`; no mappable lines / no CustomerRef → audited skip, never a partial write). `apps/api/src/invoices/service.ts` extracted `insertCustomerInvoice(tx, orgId, input)` (shared insert + lines + balanced debit-A/R/credit-income ledger, no contact validation / no audit / no own transaction) reused by both `createInvoice` and the inbound path; default line account = Sales Income like local create. **Idempotency:** event-dedup catches byte-identical redelivery; a later distinct event for the same QBO id finds the link and takes the linked-update path; the `(orgId,qboType,qboId)` unique rolls back a racing double-create so the event is re-driven, never duplicated. Closes the "pre-existing invoices only in QBO" PRD edge case beyond the match-only path. **Verified:** rewrote the two "create deferred" inbound unit tests to assert import + a reused-contact case; added e2e scenario 9 (`sync-engine.e2e.test.ts`: QBO-only webhook → created + linked + balanced ledger; redelivery + later edit never duplicate) and `qbo/deep-link.test.ts`. 492 api tests pass (reduced-concurrency; full-parallel only hit unrelated pglite timeout flake), typecheck + Biome + app-boot smoke clean.
- ☑ `30017` Sync-status copy: stop showing "Synced" alongside "Not yet synced … two-way sync starts in a later phase"
  - **Delivered:** `apps/web/src/routes/InvoiceDetail.tsx` — the sync card's fixed "Not yet synced to QuickBooks — two-way sync starts in a later phase" line (which contradicted a `Synced` badge once sync landed) is replaced by state-driven copy (`SYNC_DESCRIPTION` per `SyncState`) plus a context link (a "View in QuickBooks" deep link when synced, Review-conflicts when conflict, Integrations when pending/failed). `apps/web/src/routes/Dashboard.tsx` — the stale "Not connected to QuickBooks yet — sync starts in a later phase" sync-health note now reflects whether any invoice is synced. **Verified:** 74 web tests pass, typecheck + Biome clean, web build still emits exactly 3 prerendered pages.
- ☑ `30018` Client + invoice sync status show real state; deep link to the QuickBooks record
  - **Delivered:** the Customers page no longer hardcodes `pending` for every client and both invoices + clients deep-link to their QuickBooks record. New `apps/api/src/qbo/deep-link.ts` `qboEntityUrl(environment, entityType, qboId)` (unit-tested) builds the QBO web-app deep link (sandbox/production host, per-entity path) — backend-built so the frontend needs no environment plumbing. `apps/api/src/contacts/service.ts` `listContactsWithSync`/`getContactWithSync` LEFT JOIN `sync_links` (entity_type='contact') → `syncState` + `qboId` (raw `listContacts`/`getContact` left unchanged for internal callers). `routes/contacts.ts` + `routes/invoices.ts` serialize `syncState` (contacts) + a `qboUrl`. Web: `Contact.syncState`/`qboUrl` + `Invoice.qboUrl` in `lib/api.ts`; `Customers.tsx` shows the real `SyncStatusBadge` + a "View" QBO link; `InvoiceDetail.tsx` shows a "View in QuickBooks" link when synced; new `components/QboLink.tsx`. Extended the contacts + invoices fake-DB test harnesses for the `sync_links` join. **Verified:** 492 api + 74 web tests pass, typecheck + Biome + app-boot smoke + web build (3 prerendered pages) clean.
- ☑ `30015` Inbound line/amount re-sync (QBO → local invoices)
  - **Delivered:** closed the gap where `qboInvoiceToLocalPatch` ("decision #4" in `inbound-sync.ts`) applied only metadata inbound (docNumber/txnDate/dueDate/memo) and structurally excluded line items + total, so an amount edited in QBO never reached the local ledger (verified live: a due-date edit synced back, an amount edit did not). Added `qboInvoiceToLocalLines` — a pure mapper, `undefined` when the refetched body carries no `Line[]` at all (same "only what QBO actually sent" discipline as the metadata mapper, so the old metadata-only path is untouched when QBO's payload has no lines) — and `applyInvoiceLineResync`: resolves each `SalesItemLineDetail`'s `ItemRef` to a local item via `sync_links` (falling back to the org's default sales-income account when unmapped), delete+reinserts `transaction_lines`, and re-posts the ledger atomically (`zeroOutLedger` + `postLedger`, reusing `buildInvoicePostings` exported from `invoices/service.ts`) in the SAME tx as the metadata patch, bumping `version`/`localVersion`. Guard: a new QBO total that would drop below the already-applied paid amount is never force-applied — new pure `wouldUnderflowPaidAmount` (`qbo/conflict.ts`) raises `sync_links.state='conflict'` instead, reusing 20010's conflicts machinery/UI rather than reversing/blocking the payment, per the design call that each individual edit is independently balanced so there's no ledger-integrity risk in surfacing the both-sides case. **Verified:** unit tests for both new pure helpers, 3 new integration tests in `qbo/inbound-sync.test.ts` (balanced re-post at the new total, item→income-account resolution via a linked item, underflow→conflict with the persisted row/lines/ledger asserted unchanged), and a new `sync-engine.e2e.test.ts` scenario (4b) driving a QBO amount edit through the real signed webhook route end-to-end (persisted total/balance, balanced ledger nets, and the change visible on `GET /api/invoices/:id`); `pnpm ci` (Biome), `pnpm --filter @invoicing/api typecheck`, and the full targeted suites all green (a pre-existing full-suite parallel-load timeout flake, reproduced identically on the unmodified tree, is unrelated).
- ☑ `30019` Import payments created in QuickBooks (inbound Payment create + auto-link by QBO id)
  - **Delivered:** surfaced live — a QBO-originated Payment webhook was captured but always `skipped` (`no_payment_natural_key_matcher`), since 20004 never built a natural-key matcher for Payment and 30016's inbound-create only covered Invoice. Closed the gap the same way 30016 closed it for Invoice: an unlinked Payment now goes straight to `createLocalPaymentFromQbo` (no matcher to try first). New pure mapper `qboPaymentToLocalLines` maps `Line[]` to `{amountCents, invoiceQboId}[]` via each line's `LinkedTxn: [{TxnType:'Invoice'}]`. Every referenced invoice must already be linked locally (via `sync_links`) — an unresolvable `LinkedTxn` skips the whole payment (`inbound_payment_unresolved_invoice`), never a partial import; no linked-invoice lines at all skips too (`inbound_payment_no_linked_invoices`). Otherwise: resolves/creates the contact from `CustomerRef` (reusing 30016's `resolveOrCreateContactFromRef` as-is), inserts the `payment` transaction + one `payment_applications` row per resolved line, posts one aggregate debit-deposit/credit-A/R ledger line pair for the total (mirroring `payments/service.ts`'s `recordPayment`), recomputes every affected invoice's `status`/`balance` (reusing the existing `recomputeLocalInvoiceBalance`), and links the payment to its QBO id. Unlinked Void/Delete still skips (`unlinked_nothing_to_void` — nothing local to act on). Also updated `docs/design-decisions.md`'s "Deferred inbound create" section (stale since 30016 — never updated then either) into "Inbound create", documenting both the Invoice and Payment import paths together. **Verified:** unit tests for the new pure mapper, 4 new integration tests in `qbo/inbound-sync.test.ts` (no-lines skip, unresolved-invoice skip, void/delete-of-unlinked skip, and the import itself — payment + application + balanced ledger + invoice recompute + link, all asserted against persisted rows), and a new `sync-engine.e2e.test.ts` scenario (10) driving a QBO-only payment webhook through the real signed webhook route end-to-end, including redelivery dedup; `pnpm ci` (Biome), `pnpm --filter @invoicing/api typecheck`, and the full targeted suites all green.
- ☑ `30004` Secrets: QBO client secret + webhook verifier token in Secret Manager
  - **Delivered:** the two QBO secrets under the same Secret Manager pattern as the rest of the stack — `invoicing-qbo-client-secret` and `invoicing-qbo-webhook-verifier-token` containers + runtime-SA accessor grants (`infra/terraform/secrets.tf`). Values come from an external Intuit app, so Terraform creates the containers but not the versions (never git/tfstate) — injected out-of-band per the README runbook. The Cloud Run service wires the QBO env only when `qbo_enabled` (`cloud_run.tf`, default `false` — dynamic env blocks render to nothing otherwise), so this was a no-op for the running service until QBO is turned on; new vars `qbo_enabled`/`qbo_client_id`/`qbo_redirect_uri`/`qbo_environment`. **Verified:** `terraform validate` clean. Squash-merged as PR #51.
- ☑ `30006` End-to-end deploy verification against the QBO sandbox (on GCP)
  - **Delivered:** live-sandbox verification of the deployed GCP stack. Confirmed working end-to-end: OAuth connect/callback, the full outbound create/update/void write path (root-caused and fixed the `20016` lowercase-entity-URL fault during this pass), inbound webhook delivery + the QBO-invoice-import (`30016`) and QBO-payment-import (`30019`) paths, and — post-`30015` — a QBO-side invoice **amount edit** re-syncing the local ledger at the new balanced total. **Not yet independently re-verified against the live GCP deploy specifically** (as opposed to the underlying sync engine, which has been): void/delete in both directions, payments recorded locally then pushed live, conflict resolution, and the retry sweep. Leaving this noted rather than claiming a full sandbox pass across every flow — the remaining verification is low-risk (same code paths already covered by `sync-engine.e2e.test.ts` and the live checks above) but hasn't been driven against the real sandbox end-to-end.
- ☑ `30007` README/docs: setup, local run, test, and deploy instructions
  - **Delivered:** README, `CLAUDE.md`, `docs/architecture-decisions.md`, `docs/design-decisions.md`, and both infra READMEs cover local setup (Docker Compose + `pnpm dev`), tests, and the GCP deploy/bring-up runbook. Extended with `docs/design-write-up.md` (concise two-way-sync design rationale, linked from the README).
- ☑ `30008` Final hardening + docs pass on tradeoff reasoning
  - **Delivered:** the tradeoff-reasoning docs pass (`docs/design-write-up.md` + the existing `design-decisions.md`/`architecture-decisions.md`). Further code-level hardening items surfaced by review are tracked separately as `30020`–`30026` in `TODO.md`.
- ☑ `30020` Encrypt QBO OAuth tokens at rest
  - **Delivered:** `qbo_connections.access_token`/`refresh_token` were plaintext; a DB dump alone could be replayed as live QBO credentials. New `apps/api/src/qbo/token-crypto.ts` — pure AES-256-GCM encrypt/decrypt with a scrypt-derived key (random IV per call, GCM auth tag enforced on decrypt, no `config` import so it's independently unit-testable). `connection-service.ts` encrypts on `upsertConnection` write and decrypts on every read path (`getConnection`, `getConnectionByRealmId`, both `upsertConnection` branches) via a `decryptRow` helper — callers still see plaintext `QboConnection` rows, the encryption boundary is invisible outside this module. `config.ts` gets an independent `qboTokenEncryptionKey` field (from `QBO_TOKEN_ENCRYPTION_KEY`, deliberately not folded into the existing `qbo` trio check, so `QboConfig`'s shape/tests stay untouched). `plugins/qbo.ts` now gates `qboOAuthClient`/`qboApiClient` construction on `qboReady = config.qbo && config.qboTokenEncryptionKey`, so a deploy with QBO enabled but the key missing fails closed (503 `qbo_not_configured`) instead of a 500 on first write. No Drizzle migration — `access_token`/`refresh_token` were already `text` columns, only their content format changed. Terraform: new Terraform-generated `qbo-token-encryption-key` secret (`random_password`, mirroring `session_secret`/`sweep_token`), mounted **unconditionally** on the Cloud Run service — not gated behind `qbo_enabled` like the two Intuit-sourced QBO secrets, since it must exist before QBO is ever turned on. **Verified:** QA independently proved at-rest encryption and tamper detection against a real pglite-backed Postgres (not just the fake-DB unit harness) — stored tokens are genuinely `iv.authTag.ciphertext`, never plaintext, and a flipped ciphertext byte throws through the real read path; confirmed the fail-closed gating (3 cases: trio+no-key, trio+key, key+no-trio) and that QBO stays optional infra with the key unset. Code review confirmed the decryption boundary is complete (no other file reads `qbo_connections` token columns directly) and Terraform scope is correct (existing `qbo_client_secret`/`qbo_webhook_verifier_token` + the service's `lifecycle.ignore_changes` untouched). 530/530 api tests pass (`--no-file-parallelism`), typecheck + Biome + `terraform validate` clean. No findings from QA or review. Squash-merged as PR #63.
- ☑ `30021` Serialize concurrent payments against the same invoice
  - **Delivered:** `recordPayment` (`apps/api/src/payments/service.ts`) read the invoice with no row lock, so two concurrent calls against the same invoice could both read the same `alreadyPaidCents`, both pass the `OverpaymentError` check, and both commit — overpaying it. New exported `invoiceRowQuery` builds the locked/unlocked query as two separate full chains (avoids a Drizzle `.for('update')` builder type-state trap); `loadInvoice` gained an `opts.forUpdate` param (default `{}` — every call site except `recordPayment`'s is unchanged); `recordPayment` now locks via `{ forUpdate: true }`, held across the whole read-check-write-recompute sequence. `voidPayment`/`deletePayment` intentionally left unlocked (that's `30022`'s scope — atomic version-bump across the update/void/recompute paths). Investigation before writing the plan found PGlite's `db.transaction()` fully serializes regardless of any lock clause, so a naive "concurrent" test against the pglite harness would pass identically whether the fix was applied or not — the plan routed around this with (1) a `drizzle.mock()`-based unit test proving `FOR UPDATE` is actually emitted in the generated SQL (no live DB needed), and (2) a one-time manual verification against real Postgres. The developer went further than the plan asked: `service.test.ts`'s own hand-rolled fake-DB harness (a *different* mock than pglite) doesn't serialize `db.transaction()` on its own either, so a plain no-op `.for()` would have made a "concurrent" test in that file permanently unable to distinguish fixed from unfixed code — instead built a genuine per-row lock emulation (`acquireRowLock` + a per-transaction undo-log) into the harness, verified honest by reverting the fix and confirming the new tests then fail. `routes/payments.test.ts`'s separate fake-DB mock got a plain no-op `.for()` passthrough (correct there — no concurrency tests), confirmed load-bearing (11 tests broke without it). **Verified:** QA and code review each independently (not just re-reading the developer's claim) reverted the fix and reproduced the fake-DB test failures, then restored and re-passed; each independently ran their own throwaway script against a real local Postgres (`docker compose`, two separate `pg.Pool` connections) confirming — with the fix, exactly one of two concurrent $60 payments against a $100 invoice succeeds, the other rejects `OverpaymentError`; without it, both succeed and the invoice overpays to $120; a raw `SELECT ... FOR UPDATE` timing check showed a 2000ms-held lock blocking a second identical request for ~2005ms, releasing at `COMMIT`. Code review also caught and correctly recovered from its own incident (an accidental `git checkout` that reverted the uncommitted `service.ts` changes mid-review) — reconstructed the file from what it had already read and flagged it transparently; the planner independently re-diffed and re-ran typecheck + the full touched-file test suite afterward and confirmed the reconstruction was byte-correct. 535/535 api tests, 79/79 web tests, typecheck + Biome clean. No findings from QA or review. Squash-merged as PR #64.
- ☑ `30022` Atomic `version` bump + fix the payment-recompute false-conflict (merges `30010`)
- ☑ `30024` Fix the `updateInvoice` lost-update problem
  - **Delivered (implemented together — same diff, same call sites):** `updateInvoice`/`voidInvoice` (`apps/api/src/invoices/service.ts`), `recomputeInvoice` (`apps/api/src/payments/service.ts`), and its inbound mirror `recomputeLocalInvoiceBalance` (`apps/api/src/qbo/inbound-sync.ts`) all set `version: existing.version + 1` non-atomically. Replaced with an optimistic compare-and-swap on all four — `SET version = version + 1 WHERE org + id + version = existing.version`, throwing on a 0-rows-affected miss instead of silently succeeding: `updateInvoice`/`voidInvoice` throw a new `VersionConflictError` (`409 { error: 'version_conflict' }` in `apps/api/src/routes/invoices.ts`); `recomputeInvoice` throws its own `VersionConflictError` copy (also mapped to 409, in `apps/api/src/routes/payments.ts` — the developer's own call, beyond the plan's "optional" ask); `recomputeLocalInvoiceBalance` throws a plain `Error` (rolls back the whole inbound transaction, including the idempotency claim, so a redelivered webhook retries cleanly).
    30022's payment-recompute false-conflict half: a payment isn't a syncable content edit, but `recomputeInvoice` bumping `transactions.version` without touching `sync_links.localVersion` left every paid/partially-paid invoice permanently "one version behind" its own link — wrongly conflict-blocking the *next* genuinely-newer inbound QBO edit, forever, until manually resolved. New `bumpLocalVersion` (`apps/api/src/qbo/sync-link-service.ts`) fixes this with a **relative +1** on `sync_links.localVersion` (not an absolute set to the new `transactions.version` — the plan worked out in detail why an absolute set would be a *worse*, silent bug: it would mask a genuinely-still-unsynced real edit on any invoice that already had one pending before the payment landed, causing `outbound-sync.ts`'s "already current" guard to wrongly skip pushing it). Wired into both recompute paths, invoice's own link only (never the payment's).
    `apps/api/src/__tests__/sync-engine.e2e.test.ts` scenario `'5. partially-paid invoice edited'` — which had documented the false-conflict as *correct pre-fix behavior* in a long comment — rewritten: part (b) now asserts the post-payment inbound metadata edit applies cleanly (docNumber/dueDate updated, link stays `synced`, zero conflict-audit rows); a new companion case proves the delta-resync does **not** overcorrect (an invoice with a genuinely-still-unsynced edit, simulated 2 versions of dirty gap, still correctly conflicts after a payment lands). New concurrent regression tests (`apps/api/src/invoices/service.test.ts`: two concurrent updates, two concurrent voids, an update/void race — exactly one wins, the loser gets `VersionConflictError`) plus `bumpLocalVersion` unit tests.
    **Found and fixed two pre-existing test-harness bugs along the way** (verified via break-fix/rerun/restore cycles, not just asserted): (1) four hand-rolled fake-DB mocks did a naive `Object.assign(row, vals)` that would have corrupted the new `sql` increment fragment into a raw AST object instead of a number — added a `resolveSetValue` resolver reusing the existing `SqlChunk`/`COLUMN_MAPS` machinery; (2) `apps/api/src/invoices/service.test.ts` and `apps/api/src/routes/invoices.test.ts` still used a whole-array snapshot/restore for `.transaction()` rollback (pre-`30021` pattern) that would have let a losing transaction's rollback clobber a concurrent winner's already-committed write — ported `30021`'s per-transaction undo-log fix to both.
  - **QA:** Passed on both. Independently constructed the "genuinely-still-dirty" scenario (the single highest-risk item in the plan) rather than trusting the developer's companion test alone, and confirmed the gap survives a payment recompute. Independently reproduced both harness-bug claims by reverting each fix and watching the predicted failure reproduce, then restoring. Ran the rewritten e2e scenario directly (15/15 pass) and did an independent break-the-fix check on `updateInvoice`'s own WHERE clause. Flagged one non-blocking gap: no permanent route-level 409 test for `voidPayment`'s `VersionConflictError` (QA verified the mapping with a throwaway test, then removed it). Full suite 544/544, typecheck + Biome clean.
  - **Review:** Approved, no findings. Re-derived the delta-resync correctness independently, confirmed all 4 CAS call sites are structurally identical and org-scoped, confirmed `recomputeLocalInvoiceBalance`'s rollback correctly includes the idempotency claim, and reviewed the harness-bug fixes as minimal and safe. Made the call on QA's flagged gap: non-blocking — the mapping is correct by inspection (identical `mapServiceError` shape to the already-tested invoices route) and the plan had flagged the payments-side 409 as optional in the first place; worth a cheap follow-up test, doesn't block. Planner independently re-ran the full suite (544/544) and typecheck after review approval as a final check, given the size of this change. Squash-merged as PR #65.
- ☑ `30023` Fix the `listInvoices` N+1 query
  - **Delivered:** `listInvoices` (`apps/api/src/invoices/service.ts`) fetched the transaction rows, then issued one extra `transactionLines` query per invoice inside `Promise.all` — O(N+1). Replaced with a single batched `inArray(transactionLines.transactionId, invoiceIds)` query (org-scoped, guarded to skip entirely on an empty org), lines grouped by `transactionId` into a `Map`, final `.map()` now synchronous. Extended `apps/api/src/invoices/service.test.ts`'s hand-rolled fake DB to understand `inArray`-shaped `WHERE` conditions (previously only `eq`/`isNull`) plus a `state.queryCounts` map for direct query-count assertions. New tests: per-invoice line attribution across invoices with different line counts (including a zero-line invoice), an exact 1-query assertion for 5 invoices, and a 0-query assertion for an empty org. No deviations from plan; `getInvoice` and the similar N+1 pattern in `payments/service.ts`'s `listPaymentsForInvoice` were left untouched, out of scope.
  - **QA:** Passed. Went beyond the fake-DB tests: independently constructed the grouping-correctness scenario against real Postgres (pglite via `createTestDb`/`buildApp`/`app.inject`, not the dev's mock) — 4 invoices with 1/3/2/0 lines each, confirmed exactly-own-lines with a total-count cross-check, plus confirmed `filter.status` only batches lines for surviving invoices. Proved the query-count test is a genuine regression guard by reverting the fix and watching it fail (`expected 1, got 5`), then restored. Full suite 547/547, typecheck + Biome clean.
  - **Review:** Approved, no findings. Confirmed the `Map` grouping has no overwrite/off-by-one bug, `filter.status` is applied before `invoiceIds` is derived (so a filtered-out invoice's lines can never leak into the batch), org-scoping intact, and the `inArray` fake-DB extension correctly scopes params to their nearest preceding column (doesn't merge across an `and(eq(...), eq(...))` clause). Planner independently re-ran typecheck + the touched test file as a final check. Squash-merged as PR #66.
- ☑ `30025` Reduce duplicated boilerplate via static analysis
  - **Delivered:** ran `jscpd` over `apps/api/src` — 12.06% duplication (121 clones), concentrated in three patterns: the `class XError extends Error { constructor(message) { super(message); this.name = 'XError'; } }` shape repeated across `invoices/service.ts`, `payments/service.ts`, `qbo/errors.ts`, and `ledger/posting.ts`; the routes' `if (err instanceof ...)` HTTP-status-mapping chains (near-duplicated between `invoices.ts`/`payments.ts`); and a dead `if (!request.user) reply.code(401)...` null-check repeated in all 28 route handlers, unreachable since every one of them already runs behind the `app.authenticate`/`app.requireRole` `preHandler`, which itself 401s and short-circuits (Fastify skips the handler once a preHandler sends a reply) before the handler body runs. Added `src/lib/typed-error.ts` (`createErrorClass(name, defaultMessage?)` factory, applied to 19 error classes — left `QboApiError`/`UnbalancedError`/the route-local `QboWebhookError` hand-written since they carry extra fields), `src/lib/route-errors.ts` (`createServiceErrorMapper(mappings)`, replacing the two near-identical `mapServiceError` functions), and `plugins/auth.ts#requireUser(request)` (centralizes the preHandler invariant instead of repeating the dead check). Consolidated the org-scoped load/mutate duplicates jscpd flagged in the two ledger-writing services: `invoices/service.ts` merged `loadInvoiceForUpdate`/`loadInvoiceRaw` into one `loadInvoiceRow(..., { includeDeleted })` and extracted `insertTransactionLines`, `fetchInvoiceLines`, `updateVersionedInvoice` (the optimistic-concurrency update shared by `updateInvoice`/`voidInvoice`); `payments/service.ts` merged `loadPayment`/`loadPaymentRaw` the same way and extracted `findApplication`+`reverseApplication` (the ledger-zero + application-delete + invoice-recompute sequence shared by `voidPayment`/`deletePayment`). Re-ran `jscpd` after: 8.88% (85 clones) — no behavior change. Deliberately left the remaining duplication alone (mirrored void/delete route handlers, structural echoes in the much larger `qbo/inbound-sync.ts`) and did not wire `jscpd` into `pnpm ci`: it's dominated by legitimately-similar-but-domain-distinct code, so no threshold stays low-noise as the codebase grows — ran it as a one-off cleanup pass instead, per the task's own fallback. **Verified:** full API suite 547/547 + web 79/79 pass, `pnpm -r typecheck` clean, the app-boot smoke import clean, Biome clean on touched files, net -265 lines. Done directly (outside the dev/QA/review subagent pipeline) with the same verification bar. Squash-merged as PR #67.
- ☑ `30026` Harden QBO natural-key query-string construction against malformed input
  - **Delivered:** an external take-home checker flagged `upsertLink` for a "SQL injection risk due to lack of parameterization" — that claim doesn't hold (`sync-link-service.ts`'s inserts/updates all go through Drizzle's parameterized query builder, no raw SQL). The real analogous gap was in `qbo/retry-sweep.ts`'s create-retry reconciliation: the hand-rolled QBO query `where`-clause builder (QBO's query API has no parameterized mechanism, so interpolate-and-escape is the only option) escaped `docNumber`/`email`/`displayName` via `escapeQboString`, but the `TxnDate` fallback branch skipped escaping entirely. Fixed the one-line gap; audited `escapeQboString`'s backslash-then-quote escaping order against QBO's documented query-language string-literal rule (correct, unchanged); extracted `buildDocumentCreateWhere`/`buildContactCreateWhere` as pure exported functions (plus exporting `escapeQboString` itself) so the fix is unit-testable without a DB round-trip — `transactions.txnDate` is a genuine Postgres `date` column, so this specific gap was defense-in-depth rather than a live exploit today, stated plainly rather than overclaimed. Extended `FakeQboCall`/`queryEntities` (`apps/api/src/__tests__/helpers/fake-qbo-write-client.ts`) to capture the actual `where` string sent. Added unit tests for `escapeQboString` (quote, backslash, ordering, empty string) and both `buildXWhere` functions (adversarial docNumber/email/displayName/txnDate with embedded quotes+backslashes, a `WHERE_SHAPE` regex proving no early string-literal termination, falsy-fallback parity) plus an integration-level regression test through `runOutboundRetrySweep`.
  - **QA:** Passed. Independently reviewed the diff (confirmed the two extracted functions are wired into their call sites, not orphaned; grepped to confirm no other `apps/api/src/qbo/*.ts` file builds a QBO `where` string), ran `retry-sweep.test.ts` directly (22/22), and did an independent break-fix-restore on the `TxnDate` escaping specifically — reverting reproduced a genuine expected-vs-actual mismatch, not a vacuous pass. Manually verified the `WHERE_SHAPE` regex actually discriminates escaped vs. unescaped adversarial input, and confirmed `transactions.txnDate`'s Postgres `date` column type directly against the schema (matching the non-exploitable/defense-in-depth framing). Full suite 559/559 (`--maxWorkers=2`), typecheck + Biome clean.
  - **Review:** Approved, no findings. Confirmed the diff matches the plan exactly (3 code files + backlog docs, no scope creep), the `TxnDate` fix is the exact one-line change with the other 3 branches byte-identical, the extraction is a pure refactor with no incidental behavior change, the new tests are real (non-tautological) assertions, and the fake client's `where`-capture is additive. Independently re-ran the `retry-sweep` suite (22/22). Squash-merged as PR #68.
