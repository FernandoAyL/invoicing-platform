# Done

Completed and verified tasks. Keep the original task ID.

## Phase 0 — Design & foundations (`0000x`)

- ☑ `00001` Architecture decisions doc (`docs/architecture-decisions.md`)
- ☑ `00002` Product requirements doc (`docs/PRD.md`)
- ☑ `00003` Project instructions (`CLAUDE.md`)
- ☑ `00004` Backlog structure (`docs/backlog/`: TODO, TOCODEREVIEW, TOTEST, DONE)
- ☑ `00005` Register QuickBooks Online developer account + sandbox company
- ☑ `00006` Create QBO app: OAuth client id/secret, redirect URIs, webhook endpoint
- ☑ `00007` AWS account + IAM for Terraform; S3 remote state backend with native locking (`use_lockfile`)
- ☑ `00008` Local dev env: `docker-compose` (postgres:17 + Node 24 app) + base tooling
  - **Delivered:** `docker-compose.yml` (Postgres 17 with healthcheck + `db-data` volume; app gated on db health, source bind-mount + anonymous `node_modules` volume for hot reload), `Dockerfile` (node:24 + Corepack/pnpm), minimal `src/index.ts` (`node:http` `/health` that pings Postgres via `pg`), `package.json` + `packageManager` pin, `tsconfig.json`, `.env.example`, `.dockerignore`, and Biome (`biome.json`, 2-space to match pnpm, `format`/`lint`/`check`/`ci` scripts). Verified: `docker compose up --build` → `GET /health` → `200 {"status":"ok","db":"up"}`; `tsc --noEmit` and `biome ci` clean.
- ☑ `00009` Repo scaffolding remainder: pnpm workspace layout + typed `.env` config loader
  - **Delivered:** `pnpm-workspace.yaml` (`apps/*`, `packages/*`); backend at `apps/api` (`@invoicing/api`) with a dependency-free typed env loader (`apps/api/src/config.ts`); shared `tsconfig.base.json` extended per package; root `package.json` as workspace root. Verified: `docker compose up --build` → `/health` `200`; `pnpm -r typecheck` + `biome ci` clean.

## Phase 1 — Core app + CI (`1000x`)

- ☑ `10001` Drizzle schema + first migration — accounting core
  - **Delivered:** `apps/api/src/db/schema.ts` — 11 tables + 8 pg enums (unified `transactions` + `transaction_lines` + `ledger_entries`, `contacts` role flags, chart-of-accounts `accounts`, entity-typed `sync_links`). Money `numeric(14,2)`, uuid PKs, org-scoped FKs, self-ref `accounts.parent_id`, cascade on lines/ledger. `drizzle.config.ts` + `drizzle/0000_young_siren.sql`; `db:generate` + programmatic `db:migrate` (`src/db/migrate.ts`). Verified: migration applies over docker network (11 tables + `drizzle.__drizzle_migrations`); `tsc --noEmit` + `biome ci` clean.
- ☑ `10002` CI (GitHub Actions) + Vitest setup
  - **Delivered:** `.github/workflows/ci.yml` (push to `main` + PRs): install → `biome ci` → `pnpm -r typecheck` → `pnpm -r test` → `docker build`. Vitest wired (`apps/api` + root `test` scripts, `vitest.config.ts`); `config.ts` refactored to testable `loadConfig(env)` + `config.test.ts` (4 cases). Verified: `vitest` 4/4, `biome ci`/typecheck clean, **CI green on GitHub**.
- ☑ `10003` Fastify server bootstrap
  - **Delivered:** replaced the `node:http` placeholder with Fastify. `src/app.ts` (`buildApp`, injectable pool for tests), `src/plugins/db.ts` (fastify-plugin decorating `pool` (pg) + `db` (drizzle), owns pool lifecycle via `onClose`), `src/routes/health.ts` (`GET /health` pings the pool), `src/index.ts` (listen on `0.0.0.0:config.port`, graceful SIGTERM/SIGINT). Pino structured logging (pretty in dev via pino-pretty, JSON otherwise). Unit tests `src/routes/health.test.ts` (200 up / 503 down via `app.inject` + fake pool). **Verified:** `docker compose up --build` → `GET /health` `200`; `vitest` 6/6, typecheck + `biome ci` clean; **CI green, merged to `main` via PR #8**.
- ☑ `10004` Auth: email/password session login, httpOnly cookie, logout, seeded users, Admin/Member roles
  - **Delivered:** DB-backed opaque-token sessions (`sessions` table + migration `0001_huge_newton_destine.sql`). `apps/api/src/auth/password.ts` (scrypt N=16384/r=8/p=1, `timingSafeEqual`, never throws on malformed hash), `auth/session.ts` (256-bit token, only SHA-256 hash stored, create/find/delete, expiry enforced). `plugins/auth.ts` (`@fastify/cookie` signed with `SESSION_SECRET`; `authenticate` + `requireRole` preHandlers; cookie `httpOnly`/`sameSite=lax`/`secure`-in-prod). `routes/auth.ts` (`POST /api/auth/login` — Fastify JSON schema, dummy-hash timing defense against user-enumeration, generic `401`; `POST /api/auth/logout` — real server-side revocation, idempotent; `GET /api/auth/me`). `db/seed.ts` (idempotent org + admin/member users, `db:seed`). `config.ts`/`.env.example`/`docker-compose.yml` wired for `SESSION_SECRET`/`SESSION_TTL_HOURS`/`SEED_*_PASSWORD`; `plugins/db.ts`/`app.ts` accept an injectable `db` for route tests. **Verified:** QA ran the full containerized `docker compose up --build` flow (boots, `/health` 200, seed x2 idempotent, login→`/me`→logout→`/me` 200/200/204/401 with session revoked); `vitest` 28/28, typecheck + `biome ci` clean; **review-approved** (2 non-blocking follow-ups filed). Delivered across 2 attempts (attempt-1 QA reject: compose env passthrough).
- ☑ `10005` Contact CRUD (customer role first): name + contact info, attachable to an invoice; maps to a QBO Customer
  - **Delivered:** org-scoped contact CRUD behind `app.authenticate` (both roles). `apps/api/src/contacts/service.ts` (`createContact`/`listContacts`/`getContact`/`updateContact`/`archiveContact`, every query filtered by `orgId`); `apps/api/src/routes/contacts.ts` (`POST`/`GET` list/`GET :id`/`PATCH`/`DELETE` with JSON schemas — uuid params, email format, `minLength`, PATCH `minProperties:1`, `additionalProperties:false`). Soft-archive via a new `is_active` column (migration `0002_married_cassandra_nova.sql`), QBO-style deactivate-not-delete; archive is idempotent, list excludes archived unless `?includeInactive=true`. Also disabled Fastify's default `removeAdditional` (`ajv.customOptions.removeAdditional:false` in `app.ts`) so `additionalProperties:false` truly 400s — an app-wide fix that also tightens the 10004 login schema. No QBO `SyncLink` / audit writes (deferred to Phase 2 / `10008`). **Verified:** QA ran full CRUD + validation + idempotent-archive against the docker-compose stack as both admin & member, proved cross-org isolation with a genuine second org (GET/PATCH/DELETE/LIST all 404/exclude), and regression-checked the `removeAdditional` change (login intact, now 400s on unknown field); `vitest` 45/45, typecheck + `biome ci` clean; **review-approved** (1 non-blocking nit: whitespace-only `displayName` trims to empty).
- ☑ `10008` Audit log write path: every mutating action appends to `SyncAuditLog` (entity, action, direction, outcome, user, timestamp)
  - **Delivered:** append-only audit on every local mutation, atomic with the change. Added `'local'` to the `sync_direction` enum (migration `0003_gorgeous_wolf_cub.sql`) so app-originated actions are logged distinctly from inbound/outbound QBO sync. New `apps/api/src/audit/service.ts` (`writeAuditLog(tx, entry)` + `AuditEntry`, defaults `direction:'local'`/`outcome:'success'`). `contacts/service.ts` create/update/archive refactored to `db.transaction(tx => …)` writing one audit row each on the same `tx` (attributed to `user.id`/`orgId` from the session — not spoofable); `routes/contacts.ts` passes `user.id`. Reads and no-row-matched no-ops write nothing; `detail` carries only changed field names. Tests: `audit/service.test.ts` + `contacts.test.ts` (one row per mutation, reads/404s write nothing, forced-failure rollback leaves neither contact nor audit row). **Verified:** QA on fresh `docker compose up --build` (migration 0003 applied, `enum_range`=inbound/outbound/local) psql-confirmed one correct row per create/update/archive, cross-user attribution (member vs admin), and zero rows from reads/no-ops; `vitest` 48/48, typecheck + `biome ci` clean; **review-approved**.
- ☑ `10011` Chart of accounts: seed minimal accounts (A/R, Sales Income, bank, Undeposited Funds) + balanced posting helper that rejects unbalanced transactions
  - **Delivered:** the ledger core. `apps/api/src/money.ts` — exact integer-cents `toCents`/`formatCents` (regex-parsed, no float path; rejects `>2` decimals / malformed / non-finite). `apps/api/src/ledger/posting.ts` — `postLedger(tx, {orgId, transactionId, entryDate, lines})`, transaction-aware (posts inside the caller's txn, no HTTP surface), validates each line (no negative / both-sides / both-zero / empty set) and throws `UnbalancedError` when Σdebit ≠ Σcredit (in integer cents) **before any insert** — zero partial writes; `InvalidPostingError` on bad shape. `apps/api/src/accounts/service.ts` (`listAccounts`, `getAccountBySubtype`) + read-only `GET /api/accounts` (auth, org-scoped). `apps/api/src/db/seed.ts` seeds the 4 accounts idempotently per `(orgId, subtype)` (1000 bank / 1200 A/R / 1499 undeposited_funds / 4000 sales_income). No migration (seed + code only). **Verified:** QA — seed x2 idempotent (psql), `GET /api/accounts` 200/401, balancing rubric 69/69 (incl. float-trap `0.30=0.10+0.20`, unbalanced→error+zero rows), and a live-Postgres probe proving `postLedger` rolls back with the caller's transaction; typecheck + `biome ci` clean; **review-approved**. Unblocks 10006/10007.
- ☑ `10006` Customer-invoice CRUD: create / edit / void a `customer_invoice` Transaction with line items; post balanced `LedgerEntry` rows (debit A/R, credit income)
  - **Delivered:** `apps/api/src/invoices/service.ts` — `createInvoice`/`getInvoice`/`listInvoices`/`updateInvoice`/`voidInvoice`, org-scoped, each in one `db.transaction` covering the `transactions`+`transaction_lines` writes, the ledger posting, and a `writeAuditLog` row. Create posts **debit A/R (total) / credit income per line** via `postLedger`, `status='open'`, `balance=total`. **Immutable ledger:** edits/voids never UPDATE/DELETE a `LedgerEntry` — `zeroOutLedger` nets debit−credit per account across all prior entries and posts the negation, then (edit) reposts; re-void is a no-op. State machine gated to `open` (edit/void on void/paid → `409`, not 500); `version` increments. Line income defaults to Sales Income; explicit `accountId` resolved org-scoped. `apps/api/src/routes/invoices.ts` — `POST`/`GET` list/`GET :id`/`PATCH`/`POST :id/void`, Fastify schemas (`additionalProperties:false`, uuid/date, `minItems:1`, qty>0, price>=0), typed errors → 400/404/409/422. Money in integer cents (round per line, sum of lines). **Verified:** QA docker-compose HTTP+psql — $100 create → balanced 2-row ledger + audit; edit → totals/version updated, old rows retained, net ties to new total; void → net 0, balance 0, record kept; double-void/edit-of-void → 409; multi-line + rounding (3dp → 400); non-customer & real cross-org contact → 422, cross-org invoice → 404; unauth → 401; `vitest` 96/96, typecheck + `biome ci` clean; **review-approved** (2 non-blocking follow-ups filed).
- ☑ `10007` Payments: record a payment `Transaction` against an invoice; post ledger (debit bank / undeposited funds, credit A/R); derive paid / partial / unpaid status
  - **Delivered:** `payment_applications` join table (migration `0004_nebulous_stature.sql`) links a payment `Transaction` to the invoice it pays by applied amount — the accountant-correct model that makes status derivation a clean sum. `apps/api/src/payments/status.ts` — pure `deriveInvoiceStatus(totalCents, paidCents)` (`open`/`partially_paid`/`paid`). `apps/api/src/payments/service.ts` — `recordPayment`/`listPaymentsForInvoice`/`getPayment`/`voidPayment`, each atomic in one `db.transaction` (payment row + application + ledger + invoice recompute + audit), org-scoped. Record posts **debit deposit (default Undeposited Funds) / credit A/R**, rejects overpayment via integer-cent guard (`422`); void reverses via the shared **`zeroOutLedger`** (extracted from invoices into `ledger/posting.ts`, no `LedgerEntry` mutated/deleted) and steps the invoice status back down. `apps/api/src/routes/payments.ts` — `POST /api/invoices/:id/payments`, `GET .../payments`, `GET /api/payments/:id`, `POST /api/payments/:id/void`; typed errors → 400/404/409/422. **Verified:** QA docker-compose HTTP+psql — partial 40 → partially_paid/60 (balanced ledger + application + audit), full 60 → paid/0, overpayment 120 → 422 nothing written, pay-a-paid → 409, void → back to partially_paid/60 (ledger net 0, rows retained, application removed, audit void), double-void → 409; cross-org isolation on all 4 endpoints → 404, non-asset deposit → 422; `deriveInvoiceStatus` boundary tests real; regression: invoice edit/void on partially_paid still 409; `vitest` 136/136, typecheck + `biome ci` clean; **review-approved** (2 non-blocking follow-ups: concurrent-payment row lock, voided payments drop from list view).
- ☑ `10009` Frontend scaffold: React/Vite single app — public SSG routes (`/`, `/products`, `/pricing`) + client-rendered auth routes (`/login`, `/dashboard`) against the real `/api/auth/*`
  - **Delivered:** `apps/web` (`@invoicing/web`), Vite 8 + React 19 + react-router-dom 7 in the pnpm workspace. Public marketing pages (`src/routes/{Home,Products,Pricing}.tsx`) prerender to static HTML; auth routes (`src/routes/{Login,Dashboard}.tsx`, plus `Invoices.tsx` — a `10010` placeholder) are client-rendered behind `src/lib/RequireAuth.tsx` (loader while resolving, `<Navigate to="/login" replace>` on 401, passes the resolved user down via render-prop so `Dashboard` doesn't refetch). Same-origin httpOnly-cookie model: `src/lib/api.ts` (`login`/`logout`/`me`, `credentials:'include'`) + `src/lib/session.ts`; the frontend never reads the cookie. Dev proxy `/api → API_PROXY_TARGET` (default `http://localhost:8080`) in `vite.config.ts`. **SSG deviation (plan-authorized):** `vite-ssg` is Vue-only, so used a `vite build` + `--ssr src/entry-server.tsx` + `scripts/prerender.mjs` fallback that renders each public route via `react-dom/server` + `StaticRouter` and splices it into the client `index.html` (emits `dist/{,products/,pricing/}index.html`, deletes the intermediate `dist-ssr`). Added the `web` docker-compose service (port 5173, `API_PROXY_TARGET=http://app:8080`), a `COPY apps/web/package.json` in the root `Dockerfile`, and a web vitest+JUnit step in CI's `unit-tests` job. **QA found + fixed one real bug (surfaced only by the real browser):** `api.ts` sent `Content-Type: application/json` on the bodyless `logout()`, tripping Fastify's `FST_ERR_CTP_EMPTY_JSON_BODY` (400) so the session was never cleared — fixed client-side to send the JSON header only when a body is present (general, not a logout special-case), guarded by a red-before-green regression test (`api.test.ts`). **Verified:** HTTP QA (gates, distinct SSG artifacts, real login→me→logout→401 for both roles) **and** a real-browser Playwright pass (public pages network-free, no protected-content flash, `document.cookie` confirms httpOnly, logout now 204 + session row deleted + stays on `/login` + back-button re-guard, member login works); `vitest` 146/146 (api 136 + web 10), typecheck + `biome ci` clean, `docker build` clean; **review-approved** (attempt 2; 4 cosmetic non-blocking follow-ups filed).
- ☑ `10010` Dashboard + invoice list/detail UI with inline sync status badge (synced / pending / conflict / failed)
  - **Delivered:** the real product UI on the 10009 scaffold, consuming the 10005–10007 endpoints (server stays source of truth). **Backend (one small change):** `apps/api/src/invoices/service.ts` `getInvoice`/`listInvoices` LEFT JOIN the existing `sync_links` table (predicate bound to `transactions.orgId` + `entity_type='transaction'` + `local_id=transactions.id`, `COALESCE`d to `'pending'`) and expose `syncState` on the invoice; serialized in `routes/invoices.ts`. The `(orgId, entityType, localId)` UNIQUE constraint keeps it a single non-fanning query (no N+1), org-scoped so no cross-org leak. No migration (table + `sync_state` enum already existed) — Phase 2 sync populates data, not UI. **Frontend (`apps/web`):** extended `lib/api.ts` with typed invoice/payment/contact/account methods + `lib/money.ts`; new `components/{SyncStatusBadge,InvoiceStatusBadge,InvoiceLinesEditor,RecordPaymentDialog}.tsx`; new routes `routes/{Invoices,InvoiceDetail,InvoiceNew,InvoiceEdit,Customers,Dashboard}.tsx` (Invoices/Dashboard rewritten from the 10009 placeholders) under `RequireAuth` in `App.tsx` with a shared authed nav. Edit/Void hidden unless `status==='open'` (with graceful 409-race handling); record-payment overpayment → inline 422 with nothing written; money display-only via `formatMoney` (Dashboard totals accumulate in integer cents). **Verified:** `vitest` **190/190** (api 140 + web 50), typecheck + `biome ci` + `docker build` clean, `pnpm --filter @invoicing/web build` still emits exactly the 3 prerendered public pages. QA drove all 10 plan scenarios in a real browser (Playwright) — full invoice lifecycle (open→partially_paid→paid), void invoice + void payment, overpayment 422, empty/not-found states, member role — with `psql` confirming balanced ledger at every step, **and independently verified `syncState` against real Postgres** (pending→synced flip, `conflict`/`failed` distinct, cross-org non-leak) to cover a fake-DB-harness `.leftJoin()` deviation; **review-approved** (1 non-blocking follow-up: the test harness hardcodes the join predicate — keep the real-PG cross-org check / add a pglite harness when Phase 2 lands). Closes Phase 1.
- ☑ `10012` Design system foundation + app shell (Clearbook) — first task of the Phase-1 UI restyle
  - **Delivered:** the Clearbook design-system foundation that `10013`–`10017` build on. Self-hosted **IBM Plex Sans/Mono** (`@fontsource/*` latin subsets, imported only in `main.tsx` so the SSG public pages stay self-contained — zero external font requests). Design **tokens** in `apps/web/src/theme.ts` + `:root` vars in `apps/web/src/styles/global.css` (base/reset/custom scrollbar, plus `.ui-*` interactive-state classes that own hover/focus so an inline style can't shadow a stateful rule). Reusable **primitives** in `apps/web/src/components/ui/` (`Button` incl. `ghost`, `Card`, `Input`/`Select`/`Textarea`, `FieldLabel`, `PageHeader`, `DataState`). The **app shell** in `apps/web/src/components/shell/` (238px sidebar: brand mark+wordmark, MENU icon-nav with active pill + live Invoices count + disabled COMING SOON group + user chip w/ logout; 60px topbar: page title + search + New invoice) wired into `App.tsx`, replacing the old `AuthedLayout` — auth is now guarded **once** at the shell and `user` flows via `Outlet` context (removes a double `/api/auth/me`/double-guard). Restyled `InvoiceStatusBadge` + `SyncStatusBadge` (all 4 states; sync labels aligned to Pending/Synced/Conflict/Failed) keeping props/exports stable. **Phase-1 scope guards honoured:** topbar shows neutral "Not connected" (no fabricated QBO status); `Integrations` routes to a placeholder; no "Save & sync" introduced. **Verified:** `vitest` 190/190 (api 140 + web 50), typecheck + `biome ci` + `docker build` clean, `pnpm --filter @invoicing/web build` still emits exactly the 3 prerendered public pages; Playwright QA confirmed the shell/nav/user-chip/logout(204)+re-guard, self-hosted fonts, badges, and a strong visual match to the comp; **review-approved** (primitives called "exemplary"), with 4 small foundation follow-ups fixed before merge (dead-hover logout, search focus ring, spec-aligned sync labels, tokenized `#9aa79f`/`#f0cfc9`).
- ☑ `10013` Auth + public marketing restyle (Login + Home/Products/Pricing) to Clearbook
  - **Delivered:** the auth + public marketing surface restyled onto the 10012 foundation. New `apps/web/src/components/marketing/PublicLayout.tsx` — restyled public chrome (sticky top nav: Clearbook `Logo` + Products/Pricing links + `Sign in` CTA; footer) wrapping `/`, `/products`, `/pricing`, plus `CtaLink` (a link-styled-as-button that reuses the shared `.ui-btn` classes and stays a real `<Link>` — right element for a route change) and `MarketingSection` (centered ~1040px content column). `routes/Home.tsx` — hero (Clearbook eyebrow pill + "The invoicing platform that stays in sync" + primary/secondary CTAs) + 3 feature `Card`s. `routes/Products.tsx` — 3 product `Card`s. `routes/Pricing.tsx` — single "Standard $29/mo" pricing `Card` (mono numerals, checkmark feature list, CTA). `routes/Login.tsx` — **standalone centered branded auth card** (`Logo` + `Card` + `Input` + `Button`, inline `role="alert"` error); the `me()` already-signed-in skip, `login()`, `navigate('/dashboard')`, submit-state, and generic-error behaviour are preserved verbatim. `App.tsx` wraps the marketing routes in `PublicLayout` and renders `/login` standalone (no marketing chrome). **Phase-1 scope guards honoured:** no fabricated QBO/sync status; marketing copy describes the product as before. **Verified:** `vitest` 50/50 web (all accessible-name contracts preserved; `Pricing.test` wrapped in `MemoryRouter` like `Home.test` since the page now links — assertion unchanged), `tsc --noEmit` + `biome ci` clean, SSG build still emits **exactly the 3** prerendered pages (`/`, `/products`, `/pricing`) with the restyled content present in the static HTML and no network calls (`Login` is client-only, not prerendered); CI green (unit-tests + verify). **Live Playwright visual QA** (installed in-session) drove all four routes at 1280px + the login error path (bad creds → API down → red `role="alert"` "Invalid email or password.", values retained) — clean, strong Clearbook match; self code-review. Squash-merged as `7e909a4` (#22). Remaining restyle queue: 10014-10017.
- ☑ `10014` Dashboard restyle (greeting, KPI cards, recent invoices, sync-health) to Clearbook
  - **Delivered:** `apps/web/src/routes/Dashboard.tsx` restyled onto the 10012 primitives/shell. Greeting header ("Welcome back, <name>" + email + role; the topbar already renders the "Dashboard" title). Responsive KPI `StatTile` row (local component to the design-system "Stat/KPI tile" spec — colored dot + muted uppercase label + big mono value + colored sub): **Outstanding A/R** (brand-green mono + "N unpaid invoices"), **Open** / **Partially paid** / **Paid** counts. **Recent invoices** `Card` — up to 5 rows (mono doc number, right-aligned `tabular-nums` total, `InvoiceStatusBadge`), `.ui-table-row` hover, `View all` → /invoices, empty state ("Create your first invoice"). **Sync health** `Card` — all four `SyncStatusBadge` states with **real counts** reduced from each invoice's `syncState` (Phase-1 conflict/failed come out `0` from the data, not hardcoded), plus an honest "Not connected to QuickBooks yet — sync starts in a later phase" note → Integrations (**no fabricated connected status**, per the Phase-1 scope guards). Client-derived aggregation + `LoadingState`/`ErrorState` (`role=status`/`role=alert`) semantics preserved; **no API change** (reads the 10010 `listInvoices` + `syncState`). **Verified:** web `vitest` 50/50, `tsc` + `biome ci` clean; SSG unchanged — Dashboard is auth-gated, not prerendered — still exactly the 3 public pages; CI green. **Live Playwright visual QA** (route-stubbed `/api/auth/me` + `/api/invoices`, since it's auth-gated): populated (Outstanding A/R $2,525.50 = 1450+400+675.50; Open 2 / Partial 1 / Paid 2; 3 unpaid; sync Pending 5 / Synced 1 / Conflict 0 / Failed 0) + empty ($0.00, zeros, create-first-invoice) both render correctly inside the shell. Squash-merged as `cb3e79c` (#23). Remaining restyle queue: 10015-10017.
- ☑ `10015` Invoices list + create/edit restyle (table, filters, line-editor, sticky summary) to Clearbook
  - **Delivered:** the invoices list + create/edit surface restyled onto the 10012 primitives/shell (restyle only, **no API change**). `routes/Invoices.tsx` — filter tabs (All/Open/Partially paid/Paid/Void with live counts + active pill) + a `Card`-wrapped **CSS-grid table** (`surface-muted` faint-uppercase header; per-row mono doc#, customer, mono date, right-aligned `tabular-nums` total + balance, `InvoiceStatusBadge` + `SyncStatusBadge`, trailing chevron, `.ui-table-row` hover, whole row → detail), a per-filter empty message + the zero-invoices `EmptyState`; load/error via `LoadingState`/`ErrorState`. `components/InvoiceLinesEditor.tsx` — aligned grid line rows (Description/Qty/Unit price/**Amount** live per line + `×` remove) via the `Input` primitive (qty/price `mono`) + a "+ Add line" `Button`; **all exports + the `Line N …` aria-labels preserved** (old inline total moved into the page Summary). New `components/InvoiceSummary.tsx` — sticky right-hand **Summary** panel (Subtotal + Total from `computeDraftTotal`, inline `role=alert` error slot, submit-button slot). `routes/InvoiceNew.tsx` + `routes/InvoiceEdit.tsx` — two-column layout (Details `Card` + Line items `Card` on the left, `InvoiceSummary` on the right) via `PageHeader`/`Select`/`Input`/`Button`; New keeps the add-customer prompt + customer `<option>`s + "Create invoice" (disabled with no customers), Edit keeps the load/not-found/non-open guards + "Save changes". **Scope guard:** no "Save & sync" (never existed — labels stay "Create invoice"/"Save changes") and the New subtitle drops the sync clause ("Post a customer invoice to the ledger"). **Verified:** web `vitest` 50/50 (Invoices + InvoiceNew contracts — customer name, `$100.00`/`$60.00`, badges, empty/error, option names, line aria-labels, disabled submit, 422 inline — intact), `tsc` + `biome ci` clean; SSG unchanged — auth-gated, not prerendered — still exactly 3 public pages; CI green. **Live Playwright visual QA** (route-stubbed auth + contacts + invoices): list (populated + `Open`-filtered + empty), New (live Amount $1,000 = 10×100, Summary tracking), Edit (2 lines → Total $1,450) all correct inside the shell. Squash-merged as `8c64ed9` (#24). Remaining restyle queue: 10016-10017.
- ☑ `10016` Invoice detail + record-payment restyle (document card, payments, sync card) to Clearbook
  - **Delivered:** `routes/InvoiceDetail.tsx` + `components/RecordPaymentDialog.tsx` restyled onto the 10012 primitives/shell (restyle only, **no API change**). Detail: back link + header (mono "Invoice INV-xxxx" + `InvoiceStatusBadge` + date/due) with the same **gated actions** (Record payment primary / Edit secondary / Void danger — Edit+Void only when `open`, Record payment when `open|partially_paid`); two-column layout — left = the **document `Card`** (BILL TO customer + email, grid line-items table, then Subtotal / Total / **Paid** (= total − balance) / **Balance** totals) + a **Payments `Card`** (mono amount + date + status pill + `Void` action per row, or empty state) — right = a **Sync-status `Card`** (Phase-1 `SyncStatusBadge` + honest "not synced yet" note → Integrations, **no resolve/retry**). Void-invoice / void-payment / `actionError` (`role=alert`) + load/not-found/error states preserved. Dialog: proper **modal** (fixed `rgba(20,35,28,.28)` overlay + centered `Card` + `Input`/`Select`/`Button`) styling **only the API-backed fields** (amount `mono` prefilled to balance, date, deposit account defaulting to Undeposited Funds, memo) with the overpayment-**422 inline**; `role="dialog"`/`aria-modal`/labels/`<option>`s/`role=alert` all preserved (backdrop-click-to-close dropped — a11y lint; Cancel still closes). **Ledger-postings card DEFERRED per user directive** — no `GET /api/invoices/:id/ledger` endpoint, no API change, no faked debit/credit rows. No "Save & sync" strings. **Verified:** web `vitest` 50/50 (RecordPaymentDialog contract — amount prefill/`toHaveValue(60)`, `{type:'asset'}` + option name, record→`onRecorded`, 422 alert + dialog stays — intact), `tsc` + `biome ci` clean; SSG unchanged — auth-gated, still exactly 3 public pages; CI green. **Live Playwright visual QA** (route-stubbed): partially_paid detail (Paid $450 / Balance $1,000, payment row + Void, sync card), the record-payment modal (amount prefilled $1,000), and the open-invoice variant (Edit + Void, empty payments) all correct in the shell. Squash-merged as `f5db7ba` (#25). Remaining restyle queue: **10017 (Customers) — the last**.
- ☑ `10017` Customers restyle (table + add/edit slide-over drawer) to Clearbook
  - **Delivered:** `routes/Customers.tsx` restyled onto the 10012 primitives/shell (restyle only, **no API change**). `PageHeader` "Customers" + an **Add customer** primary button; a `Card`-wrapped CSS-grid **table** — per-row **avatar initials** chip (brand-tint), name + email/phone, **Invoices** count + outstanding **Balance** (aggregated per `contactId` from `listInvoices()`, no new endpoint — same approach as the dashboard), a Phase-1 `SyncStatusBadge state="pending"` (contacts have no `syncState` field yet; honest fixed Phase-1 state, consistent with every other screen), and an **Archive** action (real `archiveContact` delete-equivalent); `LoadingState`/`ErrorState`/`EmptyState`. **Add-customer right-side slide-over drawer** (fixed `rgba(20,35,28,.28)` overlay, 400px white panel, `shadow.drawer`, Name/Email/Phone via `Input`, Cancel/Add-customer footer, `role="dialog"`/`aria-modal`) wired for create via existing `createContact` (now also passing the API-supported `phone`). **Scope decisions (style what exists):** comp's **city** field omitted (not persisted); **row-level edit DEFERRED** — no `updateContact`/`PATCH /api/contacts/:id` endpoint exists and adding one is a backend change beyond the restyle (same call as the deferred ledger card), so Archive is the row action + the drawer is create-only. **Verified:** web `vitest` 50/50, `tsc` + `biome ci` clean, SSG unchanged — auth-gated, still exactly 3 public pages; CI green. **Live Playwright visual QA** (route-stubbed): table (avatar chips; Acme 2/$2,125.50, Globex 1/$400, Initech 0/$0; Pending badges; Archive), the right-side Add-customer drawer (Name/Email/Phone filled), and the empty state all correct in the shell. Squash-merged as `f6fa402` (#26).
  - **✅ Clearbook restyle sub-phase (10012–10017) COMPLETE** — all six tasks merged to main. `apps/web` now fully on the Clearbook design system (tokens, self-hosted fonts, shell, primitives; auth + marketing, dashboard, invoices list/create/edit, invoice detail + record-payment, customers). Deferred follow-ups worth a small future task: (1) `GET /api/invoices/:id/ledger` + the invoice-detail ledger-postings card; (2) `PATCH /api/contacts/:id` + wiring the customer drawer for edit. Next up: **Phase 2 — QBO sync engine + CD (2000x)**.

## Phase 2 — Sync engine + CD (`2000x`)

- ☑ `20001` QBO OAuth: connect/disconnect flow, token storage in `QboConnection`, automatic token refresh
  - **Delivered:** the Phase-2 foundation — the QBO OAuth connection lifecycle plus the reusable seams every later sync task (20002–20011) builds on. **Injectable `QboOAuthClient`** (`apps/api/src/qbo/oauth-client.ts`) — real Intuit OAuth2 HTTP client (Basic-auth token exchange / refresh / revoke, `application/x-www-form-urlencoded`, `redirect_uri` on code-exchange but not on refresh) behind an interface so tests inject a stub (no live Intuit in CI). **`getValidAccessToken(db, client, orgId)`** (`connection-service.ts`) — refresh-on-demand primitive: 60s expiry skew, persists rotated tokens, throws `QboNotConnectedError` leaving no half-updated row when refresh fails. **Stateless HMAC-signed `state`** (`oauth-state.ts`, `timingSafeEqual`, `SESSION_SECRET`, 10-min window, tamper/foreign-org rejection). **Admin-gated routes** `/api/integrations/qbo/{connect,callback,status,disconnect}` (`routes/integrations.ts`): connect returns the Intuit authorize URL (503 `qbo_not_configured` when unset); callback verifies `state` + cross-checks `orgId` → exchanges code → upserts the single per-org `qbo_connections` row → 302 to fixed relative `/integrations?connected=1|?error=…` (no open-redirect); status never serializes tokens; disconnect best-effort-revokes then deletes, idempotent. Optional/nullable `config.qbo` (never throws on missing `QUICKBOOKS_*`); new `plugins/qbo.ts` decorating `app.qboOAuthClient`, injectable via `BuildAppOptions`. Env `QUICKBOOKS_REDIRECT_URI`/`QUICKBOOKS_ENVIRONMENT` in `.env.example` + `docker-compose.yml`; `## QBO OAuth` note in `docs/design-decisions.md`. No schema migration (`qbo_connections`/`sync_audit_logs` already existed). The deploy-time QBO **client secret** will be injected from **AWS SSM Parameter Store** (Phase 3 / 20014); per-org OAuth tokens stay in the app DB. **Verified:** `pnpm -r typecheck` clean, `pnpm -r test` 186 api + 50 web, `biome ci` + `docker build` clean, web build emits exactly the 3 prerendered pages. QA verified non-tautological tests (token-request shape, state tamper/expiry/foreign-org, refresh-vs-stored + no-half-updated-row on refresh-throw, full HTTP matrix 401/403/200/503/302/400 `invalid_state`/idempotent disconnect, `/status` no-token-leak) + Playwright admin `status` 200 `{connected:false}` / member 403; **review-approved** (2 non-blocking follow-ups: concurrent-refresh single-flight for 20005/20006; upsert atomicity fine beyond single-admin connect). Live Intuit sandbox OAuth is user-gated (phase-end). Squash-merged as `b143d4c` (#27).
- ☑ `20002` Webhook ingestion endpoint: JSON-schema validation of inbound QBO payloads (Fastify schema), signature verification
  - **Delivered:** the inbound edge — public `POST /api/integrations/qbo/webhook` (no session; the signature is the auth), strictly receive → verify → validate → resolve realm→org → record receipt → 200 (no `SyncLink`/`Transaction` writes; refetch/dedup/apply are 20003/20005/20007). **Signature verification** (`apps/api/src/qbo/webhook-signature.ts`) — constant-time `timingSafeEqual` base64 HMAC-SHA256 over the **raw** request bytes vs `intuit-signature`. Verified via an **encapsulated Fastify content-type parser** registered only inside `apps/api/src/routes/qbo-webhook.ts` (parser + `setErrorHandler` scoped to the child plugin instance, so the global JSON parser every other route uses is untouched — locked by a `POST /api/auth/login` regression test). **Fails closed / acks fast:** no verifier token → `503 qbo_webhook_not_configured`; missing/bad signature → `401 invalid_signature`; malformed JSON → `400 invalid_json`; wrong shape → `400` via `webhookBodySchema` (`webhook-types.ts`); unknown `realmId` (via new `getConnectionByRealmId`) → `200` acked + `log.warn` with **no** audit row (anti-retry-storm); known realm → `200 {ok:true}` + one `sync_audit_logs` receipt row per entity (`direction:'inbound'`, `action:'qbo.webhook.received'`, QBO id in `triggeringEvent`/`detail`). New `QUICKBOOKS_WEBHOOK_VERIFIER_TOKEN` → nullable `config.qbo.webhookVerifierToken`, injectable via `BuildAppOptions.qboWebhookVerifierToken`/`qboPlugin` so tests sign with a known token. No schema migration; `docs/design-decisions.md` extended. **QA caught + fixed two real bugs the mocked test harness couldn't (both live-Postgres verified):** (1) `QboWebhookError` used a TS **parameter property** → Node strip-only TS crash-looped the whole API at boot (`ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX`) — rewritten to a plain field; a mandatory `docker compose up` + `/health` 200 boot check is now part of verification. (2) `local_id` (a `uuid` column) was fed Intuit's non-UUID entity id → 500 on the success path — now `localId: null` for QBO-originated events (`AuditEntry.localId` widened to `string | null`; QBO id preserved in `triggeringEvent`/`detail`). **Verified:** `pnpm -r typecheck` clean, `pnpm -r test` 205 api + 50 web, `biome ci` + `docker build` clean, web build 3 pages; live proof — correctly-signed webhook (entity id `"145"`) → 200 with psql showing exactly one audit row `local_id IS NULL`, 503/401 hold, `/api/auth/login` unaffected on the same running instance; **review-approved** (attempt 3). Non-blocking follow-ups: verifier-token/config coupling (hoist to top-level config if the webhook edge ever needs to be live before OAuth creds); per-request idempotency deferred to 20005. Live Intuit sandbox webhook delivery is user-gated (phase-end). Squash-merged as `3110501` (#28).
- ☑ `20015` Real-Postgres (pglite) integration-test harness + CI app-boot smoke
  - **Delivered:** the test/CI safety net that `20004`–`20011` build on, forced by `20002` where the hand-rolled fake-DB harness masked two real bugs (a boot-time TS-execution crash and a `uuid`-column violation), both caught only by manual live-Postgres QA. `createTestDb()`/`seedBaseOrg()` (`apps/api/src/__tests__/helpers/test-db.ts`) — in-memory PGlite (`@electric-sql/pglite`) via `drizzle-orm/pglite`, applying the **real** `apps/api/drizzle/*.sql` migrations (schema parity: enums, `uuid`, `NOT NULL`, FKs, uniques); handle cast to `NodePgDatabase<typeof schema>` so no service/`buildApp` signatures changed; cross-platform migration path via `fileURLToPath`. `test-db.test.ts` proves teeth (non-uuid into a `uuid` column throws — the fake db accepted it). Ported `audit/service.test.ts` fully + the DB-writing cases of `routes/qbo-webhook.test.ts` (success/unknown-realm/multi-notification/login-regression) to real PG; pure signature/parse/503 cases stay on the fast fake db. **CI app-boot smoke** (`.github/workflows/ci.yml`, `verify` job) — `node -e "import('./src/app.ts')"` under real Node 24 (matches the Dockerfile), dummy env, so a strip-only-TS-incompatible construct fails the pipeline (Vitest's esbuild compiles it away). `@electric-sql/pglite` added as a **devDependency** (prod image unaffected). Two-tier test-story note in `docs/architecture-decisions.md`. **Verified:** 208 api + 50 web green, `docker build` clean (+ boot smoke passes in-image); QA + review both independently proved teeth (reintroduced a parameter-property → `ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX` exit 1; reverted the `20002` `localId` fix → the ported real-PG test fails 500 where the fake passed silently). No product/route/schema/migration change; `apps/web` untouched. **review-approved.** Non-blocking follow-ups: pglite may marshal `numeric` differently from node-postgres (money-column tests on `createTestDb()` should note it); migrate remaining fake-db cases later; boot smoke covers `app.ts`'s import graph (extend if a standalone entrypoint is added). Squash-merged as `4572aee` (#29).
- ☑ `20014` CD (GitHub Actions, on merge to `main`): OIDC role → build → ECR → gated migrations (`aws ecs run-task`) → register task-def revision → `aws ecs update-service`
  - **Delivered:** the continuous-deploy pipeline. `.github/workflows/deploy.yml` (push to `main` + `workflow_dispatch`): assumes an AWS role via **GitHub OIDC** (`id-token: write`, `role-to-assume: vars.CD_ROLE_ARN`, **no static keys**) → `amazon-ecr-login` → build+push image (`docker build --target runner`, tagged `github.sha`) → register a new ECS **task-def revision** (from `describe-task-definition` piped through the render helper) → **gated migration** via one-off `aws ecs run-task` against that NEW revision (`wait tasks-stopped` + assert container `exitCode 0`; any non-zero exit `exit 1`s the job **before** `update-service`) → `aws ecs update-service` + `wait services-stable`. `concurrency: {group: deploy-main, cancel-in-progress: false}` serializes overlapping merges. All AWS identifiers via `vars.*` (region, role ARN, cluster/service/family/container, subnets, SGs, ECR repo) — no hard-coded ARNs/account-ids; the workflow never runs `terraform` and never touches Route53 (Terraform owns infra + stands up the service with `lifecycle.ignore_changes=[task_definition,desired_count]`; CD owns only revisions + the service update, per `docs/design-decisions.md` §"Deploy and IaC boundary"). **Register-before-migrate is deliberate** — migrations must run against the new image or the release's own migration files are skipped; registration is metadata-only and moves no traffic. `Dockerfile` made multi-stage: new prod **`runner`** target (`pnpm install --prod --frozen-lockfile --filter @invoicing/api`, ships `apps/api/drizzle/` migrations, `CMD … start`); the **`dev` stage stays the default target** so docker-compose and CI's `docker build` are unchanged. `apps/api/src/deploy/render-task-def.ts` — pure `renderTaskDef({template,image,containerName})` (`structuredClone`, throws on missing container, strips ECS read-only fields `taskDefinitionArn/revision/status/requiresAttributes/compatibilities/registeredAt/registeredBy/deregisteredAt`) + a `pathToFileURL`-guarded CLI, invoked the same way as `db:migrate` (Node 24 native TS). **Verified:** 210 api (incl. 5 render-task-def cases) + 50 web tests pass, `biome ci` + `pnpm -r typecheck` clean, `actionlint` clean, both Docker targets build (dev default unbroken). QA confirmed the migration gate fails-closed (no false-green where a failed/absent migration lets `update-service` run — JMESPath `[0]` on a missing exitCode → `"None"` ≠ `"0"` → job fails), register-before-migrate coherence, and the no-secrets/no-terraform/no-Route53 boundary; **review-approved** (confidence: no scope creep; the render helper strips exactly what `register-task-definition` rejects; runner correctly excludes the drizzle-kit devDep since runtime `db:migrate` uses `drizzle-orm/node-postgres/migrator`). **Not runnable E2E against AWS yet** — target ECR/ECS/RDS/OIDC infra is Phase 3 (`30001`/`30009`/`30005`); GitHub `vars`/`secrets` populated there. Non-blocking follow-ups (file against `30001`): add `USER node` to the runner stage; inspect `run-task` `failures[]` for clearer placement-failure logs; enable ECS deployment circuit-breaker rollback. Squash-merged as `d49192f` (#30).
- ☑ `20003` Refetch: when a webhook payload is incomplete, fetch full invoice/payment state from QBO before applying
  - **Delivered:** the QBO **data-API read client** + `refetchEntity` primitive that `20004`/`20007` consume — a read-only library primitive (no route, no webhook wiring, no DB writes; applying is 20007). `apps/api/src/qbo/api-client.ts` — injectable `QboApiClient`; `getEntity()` does `GET /v3/company/{realmId}/{Type}/{id}?minorversion=73` (base URL per `config.qbo.environment` sandbox/production, `Authorization: Bearer` + `Accept: application/json`), unwraps the `{Invoice:{...}}` envelope. **Typed error taxonomy for 20011:** 401→`QboAuthError`, 404→`QboNotFoundError`, 429/5xx→`QboApiError{retryable:true}`, other non-2xx/malformed→`{retryable:false}`; bearer never logged/thrown. `apps/api/src/qbo/refetch.ts` — `refetchEntity()` composes `getValidAccessToken` (20001 refresh-on-demand) → `getEntity`; `mapNotificationToEntityType` (pure). New typed errors in `qbo/errors.ts` (plain-field constructors — no TS parameter properties). `qboApiClient` injection seam in `plugins/qbo.ts` + `app.ts` mirroring `qboOAuthClient`. **Verified:** 224 api (208→224) + 50 web green, `biome ci` + typecheck clean, `docker build` clean, CI **boot smoke** exit 0, web build 3 pages. `api-client.test.ts` (fake fetch — URL/headers per env, envelope unwrap, full error matrix, bearer-leak check), `refetch.test.ts` (`createTestDb()` real pglite — token-refresh path asserts the refreshed token reaches `getEntity`, no-connection, typed-error propagation, notification mapping). **review-approved.** Non-blocking follow-ups (downstream): retryable set omits 408/425/509; `QboEntityEnvelope` untyped (20004/20007 narrow); one bearer-leak sub-assertion sits inside a catch. Rebased past the parallel `20014` CD merge (disjoint). Squash-merged as `af69437` (#31).
- ☑ `20004` Mapping layer: entity-typed `SyncLink` resolution (`Contact` / `Account` / `Item` / `Transaction` ↔ QBO id + type), including chart-of-accounts / GL accounts
  - **Delivered:** the heart of the sync model — the DB-backed mapping service over the existing `sync_links` table + pure natural-key matchers that outbound (`20006`), inbound (`20007`), and idempotency (`20005`) build on. `apps/api/src/qbo/sync-link-service.ts` (org-scoped, `db`-first so it composes in a `tx`): `resolveQboType(entityType, txnType?)` (`contact→Customer`, `account→Account`, `item→Item`, `transaction+customer_invoice→Invoice`, `+payment→Payment`, else `UnmappableEntityError`); `findLinkByLocal`/`findLinkByQbo` (org-scoped); `upsertLink` — idempotent select-then-branch inside `db.transaction`, respects both uniques (`unique(orgId,entityType,localId)`, `unique(orgId,qboType,qboId)`), throws `ConflictingLinkError` on a local→different-qbo or qbo→different-local relink (never a silent overwrite); `setLinkState`/`markSynced`/`markConflict`/`markFailed`; `resolveTransactionDeps` — reference-data-first dependency **report** (contact + distinct line accounts + distinct line items → `allLinked`/`unlinked`, `DepStatus.link` carries state) that 20006 calls before pushing a document. `apps/api/src/qbo/natural-key.ts` — pure tri-state matchers (`match`/`none`/`ambiguous`, never auto-links ambiguous): contact by normalized email (case/space-insensitive) with displayName fallback only when no email; invoice by docNumber+total+date (or total+date+customer without a docNumber); money compared via `toCents` (integer cents, never float). `apps/api/src/qbo/errors.ts` — `UnmappableEntityError`, `ConflictingLinkError` (plain-field constructors, Node-24 boot-smoke safe). `docs/design-decisions.md` Mapping section extended. **No route, no schema, no migration** — mapping library only; QBO query-API candidate *fetching* deferred to inbound/reconciliation (`20007`). **Verified:** re-ran full pipeline after taking over an interrupted session — 264 api (incl. new sync-link + natural-key suites, 35 cases) + 50 web tests pass, biome + typecheck clean, `docker build` clean, Node-24 boot smoke `boot-ok`, web build still exactly 3 prerendered pages. QA proved the conflict/idempotency/org-isolation/money-as-cents assertions are non-tautological; **review-approved** (clean, correct, org-scoped, no scope creep). Non-blocking follow-ups: optionally map raw PG `23505` → `ConflictingLinkError` for the concurrent same-pair race (DB uniques already prevent any bad row; single-writer sync); `20006` must decide whether a non-`synced` reference link gates the document push. Squash-merged as `b6cbfad` (#32).
- ☑ `20005` Idempotency: event dedup by external event id, idempotency keys on writes, `ON CONFLICT` upserts — no duplicate records or repeated writes
  - **Delivered:** the idempotency layer so duplicate/redelivered QBO webhooks never create duplicate records or repeated writes. New `processed_events` table (`apps/api/src/db/schema.ts` + drizzle-generated `0005_wakeful_silver_samurai.sql`; `unique(org_id, event_key)`, FK → `organizations`, `(org_id)` index). `apps/api/src/qbo/event-dedup.ts`: `buildEventKey({realmId,name,id,operation,lastUpdated})` (pure tuple `realmId:name:id:operation:lastUpdated`, 4-tuple fallback when `lastUpdated` absent) + `recordEventIfNew(db, …)` — **atomic** `INSERT … ON CONFLICT (org_id, event_key) DO NOTHING RETURNING id` (no separate SELECT, so concurrent double-fires can't both win); returns `true` on first delivery, `false` on redelivery. Wired into `apps/api/src/routes/qbo-webhook.ts` per entity **before** the audit-write: first delivery writes the existing `qbo.webhook.received` row; a duplicate writes one `qbo.webhook.duplicate`/`outcome:'skipped'` row and is skipped (still acks `200`; unknown-realm ack-and-skip unchanged). Pure `outboundIdempotencyKey({orgId,entityType,localId,localVersion})` = `orgId:entityType:localId:v<localVersion>` (`apps/api/src/qbo/idempotency-key.ts`) for outbound sync (`20006`). `docs/design-decisions.md` §Idempotency updated. **Verified:** 280 api (incl. new dedup, key, and end-to-end webhook-dedup suites with anti-tautology row-count assertions — same body twice → exactly one `received` row; genuine re-edit with a new `lastUpdated` NOT deduped; per-entity dedup in a multi-entity notification) + 50 web tests pass, biome + typecheck clean, `docker build` clean, Node-24 boot smoke `boot-ok`, migration applies via `createTestDb()`. **Review-approved.** Non-blocking follow-ups carried into 20006/20007/20011 briefs: (1) the dedup insert + apply are separate statements — 20007/20011 must claim + apply in ONE `db.transaction` (`recordEventIfNew` accepts a `tx`) so a mid-apply crash lets Intuit's redelivery re-drive rather than being deduped away; (2) `buildEventKey` uses unescaped `:` (safe for QBO numeric ids + terminal `lastUpdated`; escape if reused with free-text); (3) `lastUpdated`-absent fallback can falsely dedup two distinct edits — don't treat inbound dedup as a hard guarantee for lastUpdated-less payloads. Squash-merged as `099fbe8` (#33).
- ☑ `20006` Outbound sync: propagate internal create/edit/void of invoices and payments to QBO
  - **Delivered:** the outbound half of the sync engine. `apps/api/src/qbo/outbound-sync.ts` — invoked **best-effort after the local `db.transaction` commits** (never inside it): reference-data-first + **`synced`-gated** (`ensureEntitySynced` pushes contact + each distinct line account/item only when their `SyncLink` isn't already `synced`; a `pending`/`failed` ref is treated as unlinked and re-pushed before the document — resolves the 20004 review note); **outbound safety** via create-vs-update by existing `SyncLink` (a retried push issues a sparse UPDATE with Id + stored SyncToken — refetched via `getEntity` when the cached token is missing — never a duplicate CREATE); **void = QBO `?operation=void`** (never delete; a never-synced void is a safe skip); **failure isolation** — any QBO/mapping error → link `failed` + `outbound/failure` audit + `{status:'failed'}`, the local write + HTTP response never affected; **no connection → total no-op** (link stays `pending`). `markSynced`/`upsertLink` persist `qboId` + `qboSyncToken` + `localVersion` + `lastSyncedAt`. Extended `apps/api/src/qbo/api-client.ts` with `createEntity`/`updateEntity`/`voidEntity` (reusing the 401→auth / 404→notfound / 429/5xx→retryable mapping). `routes/invoices.ts` + `routes/payments.ts` wired best-effort; `docs/design-decisions.md` §Mapping/§Idempotency/§Delete-vs-void updated. Tests use an injectable fake QBO write client + `createTestDb()`. **Verified:** 311 api (incl. new outbound-sync, payload-builder, client-write, and route-outbound suites — idempotency create-count stays 1 on re-push, failure-isolation with surviving local row, ref-gating, void/never-synced-noop, no-connection-noop, payment LinkedTxn, ConflictingLinkError→failed) + 50 web tests pass, biome + typecheck clean, docker build clean, Node-24 boot smoke `boot-ok`. QA run by planner (QA subagent hit a transient session limit); **review-approved** — all three hard guarantees hold (network outside the txn, no throw escapes to the handler, no duplicate QBO record). Non-blocking handoffs to later tasks: (1) **20011** — a first-ever push failure leaves the link `pending` not `failed` (domain create seeds no link row; `markFailed` is UPDATE-only), so retry must scan `pending`/no-synced-link or seed a link at create; (2) **20008/20011** — create-then-persist duplicate window (mitigate with a claimed/in-flight link before push and/or 20007 natural-key reconciliation before CREATE); (3) routes await the push before responding — decouple via queue/fire-and-forget in the retry task; (4) `buildQboPayment` sends `TotalAmt = txn.total` — note for the live-sandbox pass. Squash-merged as `f4ed324` (#34).
- ☑ `20007` Inbound sync: apply QBO-originated changes to internal records
  - **Delivered:** the inbound half of the sync engine + the correctness fix carried from 20005's review. `routes/qbo-webhook.ts` restructured so each entity is processed as **refetch full QBO state (`refetchEntity`, OUTSIDE any tx) → ONE `db.transaction` claiming (`recordEventIfNew(tx,…)`) and applying (`applyInboundEntity(tx,…)`) atomically** — a crash after refetch but before commit rolls back the dedup claim so Intuit's redelivery re-drives the event (no dropped events, no network inside a tx); a failed refetch writes a `failure` audit and leaves the event **unclaimed** (retryable). `apps/api/src/qbo/inbound-sync.ts` — `applyInboundEntity` + pure mappers (`qboInvoiceToLocalPatch`/`qboPaymentToLocalPatch`, which **structurally exclude amount/line/status fields** so an Update can never unbalance the ledger) + org-scoped candidate loaders. **Apply matrix:** linked Update → patch QBO-owned metadata (DocNumber/TxnDate/DueDate/memo); linked Void/Delete → local void (zero ledger, delete `payment_applications`, recompute invoice status/balance — logic verified byte-for-byte equivalent to canonical `voidInvoice`/`voidPayment`, and more robust: recomputes every affected invoice); unlinked → natural-key link (single confident match only, via the 20004 matchers with a role-reversal alias) or `skipped` audit (never auto-create, never guess); inbound CREATE of brand-new local rows **deferred** (documented, recorded as `no_match:create_deferred` for manual linking); Merge/Emailed/unsupported-type → skipped. `markSynced` stamps SyncToken + localVersion (the compare seam for 20008/20010). `docs/design-decisions.md` §Idempotency/§Mapping/§Failure-handling updated. Tests use `createTestDb()` + injectable fake apiClient (canned refetch payloads). **Verified:** 345 api (incl. 32 new inbound-sync + 14 webhook tests — atomicity proven by a mid-apply throw leaving 0 `processed_events` rows via the shared tx, idempotent redelivery proven by unchanged persisted state, full apply matrix with real row/value assertions, ledger balanced/zeroed on void) + 50 web tests pass, biome + typecheck clean, docker build clean, Node-24 boot smoke `boot-ok`. **Review-approved** (the hand-rolled inbound void/recompute is faithful to canonical service logic; no scope creep). Non-blocking carry-forwards: (20010) inbound Void of a locally *paid* invoice leaves the applied payment intact → contact A/R net-negative (the voided-in-both conflict case — 20010 decides whether to reverse/flag the payment); (20008) the link's `localVersion` snapshot lags the txn row by one after a link+patch — normalize in the stale-skip comparison; (boundary) metadata TxnDate patch doesn't re-post the ledger `entryDate` (intended per the metadata-only scope). Squash-merged as `d77db20` (#35).
